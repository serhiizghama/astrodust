## 1. Общий вид денежной суммы

- [ ] 1.1 Перенести просвет между значком и числом из `HUD.counterGap` в `UI`
      (`src/config.ts`), удалить `counterGap` из `HUD`
- [ ] 1.2 Завести `src/render/credits.ts`: `CREDITS_TONE`, `measureCredits`,
      `drawCredits(ui, amount, x, y, style)` — значок монеты, просвет, число;
      выравнивание `left/center/right` по всей группе, возврат ширины
- [ ] 1.3 Вертикальное положение значка считать из кегля строки и высоты
      спрайта, а не подставлять числом по месту

## 2. Счётчик в углу кадра

- [ ] 2.1 Переписать `drawCredits` в `src/render/hud.ts` через общий вид:
      убрать локальный `CREDITS_COLOR`, импорт `COIN_ICON`/`CURRENCY_ICON`
      и ручное измерение
- [ ] 2.2 Убедиться, что счётчик остаётся с ореолом, прижат к правому краю
      и стоит на прежнем месте относительно угла

## 3. Оверлей исследований

- [ ] 3.1 Счёт в заголовке (`src/render/overlay.ts`) — общим видом
      заголовочным кеглем, без знака `₡`
- [ ] 3.2 Цена под узлом — общим видом мелким кеглем, тон по-прежнему от
      состояния покупки; ширину суммы учитывать при подгонке под шаг колонки
- [ ] 3.3 Цена в полосе сведений — общим видом кеглем полосы, выравнивание
      по правому краю панели
- [ ] 3.4 Проверить, что подпись «открыта» осталась текстом: это состояние,
      а не сумма

## 4. Причина недоступности — значением, а не строкой

- [ ] 4.1 Ввести `TechNote` и переписать `statusNote` в
      `src/progress/research.ts`: возвращает `none` / `short` с суммой /
      `locked` со списком предпосылок; знака `₡` в модуле не остаётся
- [ ] 4.2 Обновить публичную поверхность `src/progress/index.ts`
- [ ] 4.3 Изменить тип `OverlayNode.note` на `TechNote` и передавать значение
      из `src/main.ts` без форматирования
- [ ] 4.4 Собирать строку причины в полосе сведений: слова + сумма общим
      видом; обрезается по месту слово, а не сумма

## 5. Проверки

- [ ] 5.1 `tests/ui-layer.ts`: сумма — значок и число одним тоном и в одном
      порядке; ширина измеряется; выравнивание по правому краю держится при
      смене числа знаков
- [ ] 5.2 `tests/ui-layer.ts`: ни одна надпись кадра не содержит знака `₡`
      (кадр с открытым оверлеем и видимой панелью)
- [ ] 5.3 `tests/game-hud.ts`: счётчик рисуется тем же значком, что и суммы
      оверлея
- [ ] 5.4 `tests/research.ts`: у каждого некупленного узла сумма нарисована
      значком и числом; тон цены различается по состоянию; недостающая сумма
      в полосе сведений показана тем же видом
- [ ] 5.5 `tests/research.ts`: проверки `statusNote` переведены на `TechNote`
- [ ] 5.6 Прогнать `npm run typecheck` и `npm test`

## 6. Внешний вид

- [ ] 6.1 Пересмотреть снимки `npm run shot:ui`: счёт, цены под узлами и
      полоса сведений обозначены одинаково
- [ ] 6.2 Сверить стоимость кадра интерфейса с прежним замером: лишние значки
      не должны утаскивать кадр за шаг
- [ ] 6.3 Обновить шапки затронутых модулей: убрать из `hud.ts` и `overlay.ts`
      упоминания собственного обозначения валюты
