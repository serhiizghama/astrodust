## 1. Ввод

- [x] 1.1 `src/core/input.ts`: признак `menuClosePressed` — нажатие `Escape`
      в этом шаге. `GAME_KEYS` НЕ трогать: `Escape` остаётся у браузера,
      и это оговорено в шапке признака
- [x] 1.2 `src/core/input.ts`: `releaseAll()` снимает и разовые нажатия —
      `pressed` и `mouseLeftPressed`, а не только удержания. Обновить его
      шапку: сброс полный и общий у потери фокуса и переключения меню

## 2. Крестик: числа, значок, геометрия

- [x] 2.1 `src/config.ts` → `TECH_TREE`: сторона кнопки закрытия и просвет
      до счёта кредитов. Сторону вывести от высоты заголовка (`UI.line + 2`)
      комментарием — иначе её подкрутят вслепую и раскладка поедет
- [x] 2.2 `src/render/sprites/icons.ts`: спрайт крестика и его размер рядом
      с `POINTER`
- [x] 2.3 `src/render/overlay.ts`: `closeButtonRect(layout)` и
      `overClose(x, y, layout)` — из той же `TechTreeLayout`, что и узлы
- [x] 2.4 `src/render/index.ts`: вывести `overClose` (и тип цели, если он
      окажется нужен снаружи) в публичную поверхность

## 3. Логика меню

- [x] 3.1 `src/progress/research.ts`: `OverlayInput` получает
      `menuClosePressed`
- [x] 3.2 `src/progress/research.ts`: цель курсора — `number | 'close' | null`
      вместо номера узла; покупка мышью происходит, только когда цель — номер
- [x] 3.3 `src/progress/research.ts`: `close()` и предикат
      `closeRequested(input, target)` — `Escape` или нажатие по крестику

## 4. Проводка в шаге

- [x] 4.1 `src/main.ts`: цель курсора в открытом меню считается в начале шага
      одной раскладкой (крестик проверяется раньше узлов) и служит и закрытию,
      и покупке
- [x] 4.2 `src/main.ts`: состояние оверлея меняется ОДНИМ стыком до раздачи
      ввода — `T` переключает, `closeRequested` закрывает; любой переход
      сбрасывает ввод и гасит наведение
- [x] 4.3 `src/main.ts`: `overlayView()` сообщает, наведён ли курсор на крестик

## 5. Отрисовка

- [x] 5.1 `src/render/overlay.ts`: `OverlayView` получает признак наведения
      на крестик
- [x] 5.2 `src/render/overlay.ts`: крестик рисуется в заголовке — подложка,
      значок, подсветка наведения; счёт кредитов сдвинут влево на ширину
      кнопки с просветом
- [x] 5.3 `src/render/overlay.ts`: строка подсказки называет оба клавиатурных
      способа и подгоняется по измеренной ширине

## 6. Проверки

- [x] 6.1 `tests/game-shell.ts`: `Escape` читается ровно один шаг и НЕ
      подавляется браузеру
- [x] 6.2 `tests/game-shell.ts`: сброс ввода снимает разовое нажатие — после
      `releaseAll()` «нажата в этом шаге» ложно и для клавиши, и для кнопки
- [x] 6.3 `tests/research.ts`: `Escape` закрывает открытый оверлей и не
      открывает закрытый
- [x] 6.4 `tests/research.ts`: нажатие по крестику закрывает; нажатие по нему
      и `Escape` не списывают кредитов и не открывают технологий при заведомо
      достаточном счёте
- [x] 6.5 `tests/research.ts`: кнопка закрытия лежит внутри панели, не
      пересекает поле дерева и попадание в неё имеет приоритет над узлами —
      на опорном и на максимальном кадре
- [x] 6.6 `tests/ui-layer.ts`: в кадре открытого оверлея есть значок крестика;
      счёт кредитов не наезжает на кнопку; наведение на крестик меняет его вид
- [x] 6.7 `npm run typecheck` и `npm test` зелёные
- [x] 6.8 `npm run shot:ui` — снимок оверлея с крестиком, проверить глазами
