## 1. Токены вида и цвет

- [x] 1.1 Добавить в `src/palette.ts` получение цвета набора с прозрачностью
      в виде строки для канваса; убедиться, что новых литералов цвета в коде
      не появляется
- [x] 1.2 Добавить в `src/config.ts` блок `UI`: шкала кеглей (мелкий,
      основной, заголовочный), высота строки, радиусы скругления, толщина
      обводки, радиус и смещение тени, степени прозрачности, отступы — всё
      в ячейках кадра
- [x] 1.3 Собрать токены вида интерфейса (заливка панели, обводка, тень,
      текст, свечение, состояния слота и узла) в одном месте слоя интерфейса,
      выводя их из `RAMP` и блока `UI`

## 2. Слой интерфейса

- [x] 2.1 Завести подсистему `src/render/ui/` с `index.ts` как единственным
      входом; описать в шапке границы слоя (координаты — ячейки кадра, перевод
      в пиксели устройства свой, матрица контекста единичная)
- [x] 2.2 Описать поверхность `UiSurface`: подложка, текст, линия связи,
      значок, измерение строки, подгонка строки под ширину
- [x] 2.3 Реализовать `CanvasSurface` поверх `CanvasRenderingContext2D`:
      перевод координат по множителю `S`, кегли и толщины в пикселях
      устройства, мягкая тень, скруглённый прямоугольник, градиент подложки
- [x] 2.4 Реализовать `RecordingSurface`: журнал операций и детерминированная
      метрика текста
- [x] 2.5 Реализовать вывод значка: растеризация спрайта в офскрин один раз,
      кэш по «спрайт + палитра», вывод целым множителем с выключенным
      сглаживанием
- [x] 2.6 Реализовать подгонку строки: обрезка с многоточием по измеренной
      ширине
- [x] 2.7 Написать `tests/ui-layer.ts` на `RecordingSurface`: кегли из шкалы,
      пустая строка ничего не рисует, обрезка длинной подписи, выравнивание
      вправо при смене числа знаков, центрирование при разных метриках,
      токены подложек из общего набора, цвет тени и цвета интерфейса выводимы
      из `RAMP`

## 3. Экранный буфер и вывод кадра

- [x] 3.1 Перевести `src/core/display.ts` на экранный буфер: `canvas.width =
      ceil(cssW · dpr)`, мир — отдельный офскрин размером с буфер кадра,
      `fitFrame` не меняется
- [x] 3.2 Переписать `present()`: `putImageData` в офскрин мира и `drawImage`
      на экран с выключенным сглаживанием и множителем `scale · dpr`
- [x] 3.3 Оставить `clientToBuffer` в ячейках кадра без изменений и убедиться,
      что попадание курсора не поехало
- [x] 3.4 Убрать `image-rendering` из `index.html`, объявить семейство шрифтов
      интерфейса (системный стек без засечек) и сглаживание текста
- [x] 3.5 Дополнить `tests/game-shell.ts`: экранный буфер учитывает плотность
      экрана, множитель и разрешение буфера от неё не зависят

## 4. Панель действий и строки состояния

- [x] 4.1 Переписать отрисовку панели в `src/render/hud.ts` на `UiSurface`:
      скруглённая подложка с тенью, слоты, значки, подписи клавиш
- [x] 4.2 Развести состояния слота двумя средствами: заливка + обводка
      у обычного и пустого, заливка + свечение у активного, отличимая от него
      подсветка наведения
- [x] 4.3 Перевести счётчик кредитов: значок, число, тень или подложка под
      надписью, неподвижный правый край при смене числа знаков
- [x] 4.4 Перевести строки состояния над панелью (инвентарь, вид постройки,
      причина отказа) с подгонкой под свободную ширину
- [x] 4.5 Оставить `hudLayout`, `slotAtPoint`, `overBar` без изменений
      сигнатур; перевести размеры в ячейки кадра там, где они были «в пикселях»
- [x] 4.6 Переписать проверки кадра в `tests/game-hud.ts` с пикселей буфера
      на журнал `RecordingSurface`; проверки раскладки и попадания курсора
      оставить как есть

## 5. Оверлей исследований

- [x] 5.1 Переписать `src/render/overlay.ts` на `UiSurface`: панель, заголовок
      со счётом, узлы, подписи, полоса сведений, строка управления
- [x] 5.2 Заменить колено из `hLine`/`vLine` на сглаженную линию связи
      со скруглённым поворотом; сохранить различие ребра из открытой
      и неоткрытой предпосылки
- [x] 5.3 Развести признаки узла: обводка — вид эффекта, заливка — состояние
      покупки; кольца выбора и наведения — свечением поверх
- [x] 5.4 Подписи узлов подгонять под шаг колонки обрезкой с многоточием;
      пересчитать `TECH_TREE.rowStep` и высоту полосы сведений от новой высоты
      строки
- [x] 5.5 Перевести курсор меню на слой интерфейса, сохранив совпадение остриё
      ↔ точка попадания
- [x] 5.6 Оставить `techTreeLayout`, `nodeOrigin`, `nodeAtPoint`, `gridOf`
      без изменений сигнатур
- [x] 5.7 Переписать проверки отрисовки в `tests/research.ts` на журнал;
      добавить проверку обрезки длинной подписи и помещаемости дерева
      в опорный и максимальный кадр

## 6. Подключение и снос старого

- [x] 6.1 Развести кадр в `src/render/renderer.ts`: мир в буфер, `present()`,
      затем интерфейс через слой; перенести отладочный слой туда же
- [x] 6.2 Обновить шапки-инварианты `renderer.ts`, `hud.ts`, `overlay.ts`,
      `display.ts` под новый порядок кадра
- [x] 6.3 Удалить `src/render/font.ts` и его экспорты из `src/render/index.ts`
      (`drawText`, `textWidth`, `hasGlyph`, `glyphChars`, `GLYPH_H`, `LINE_H`,
      `TEXT_SHADOW`)
- [x] 6.4 Удалить `hLine`/`vLine` из `src/render/draw.ts` и упоминание
      «сглаживание запрещено целиком» из его шапки
- [x] 6.5 Удалить `tests/pixel-text.ts` и ссылки на него в `tests/run.ts`
- [x] 6.6 Обновить `tests/architecture.ts` под подкаталог `render/ui/`
      и прогнать: глубоких импортов и рёбер вверх нет
- [x] 6.7 Обновить `tests/shot.ts` и `tests/harness.ts`: снимок остаётся
      снимком мира, заглушка `Display` соответствует новому устройству

## 7. Проверка

- [x] 7.1 `npm run typecheck` и `npm test` — зелёные
- [x] 7.2 `npm run shot` — снимок мира совпадает с прежним пиксель в пиксель
      (интерфейса в нём больше нет)
- [x] 7.3 Снимок интерфейса в браузере через Playwright: панель, оверлей,
      счётчик на светлом и тёмном фоне мира; сложить в `shots/`
- [x] 7.4 Замер стоимости кадра в браузере с открытым оверлеем и без него;
      при просадке — сузить тени до крупных подложек
- [x] 7.5 Проверить кадр на плотности экрана 1 и 2 и на дробной: мир
      квадратными пикселями, текст сглажен, панель на месте
- [x] 7.6 Подобрать токены (кегли, радиусы, прозрачности, тени) по снимкам
      и зафиксировать в `config.ts`
- [x] 7.7 `npm run format` и `npm run build`
