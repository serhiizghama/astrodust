## 1. Таблица материалов

- [x] 1.1 В `src/world/materials.ts` снять `blocksPlayer` у рыхлого реголита,
      пульпы, иридия и шлака; в шапке файла записать правило одной строкой:
      блокирует нетронутый массив и корпус модуля, всё подвижное проходимо
- [x] 1.2 Там же переписать вывод доли выработки (`base.yieldRate`, 0.35):
      число остаётся, но опирается теперь на то, что выработке есть куда
      падать, а не на непролезание персонажа в засыпанный ход
- [x] 1.3 Проверить комментарии у изменённых веществ: строки про «держит
      персонажа» у реголита, пульпы, иридия и шлака удалить или заменить
      утверждением, которое ещё верно

## 2. Персонаж

- [x] 2.1 Удалить из `src/entities/player.ts` продавливание:
      `pushThroughLoose`, `push`, поле `pushTimer` и вызов из `update`
- [x] 2.2 Убрать ставшие ненужными импорты (`MatterState`, `MAT_STATE`,
      если после удаления их никто не читает)
- [x] 2.3 Удалить `PLAYER.pushInterval` из `src/config.ts` и убедиться
      грепом, что читателей не осталось

## 3. Проверки поведения

- [x] 3.1 `tests/pixel-world.ts`: заменить проверку «рыхлый реголит держит
      персонажа» на «ни одно сыпучее не блокирует»; добавить проверку полного
      состава блокирующих веществ (порода, глубинная порода, спёкшийся
      реголит, лёд, корпус модуля) — она и есть страж правила
- [x] 3.2 `tests/player-movement.ts`: удалить набор проверок продавливания
      (скорость сквозь завал, «порода не продавливается» оставить как
      «массив не раздвигается», «не тонет в куче» — удалить как отменённое
      поведение); добавить: падение сквозь кучу до породы, свободный ход
      внутри реголита, сохранность числа ячеек реголита после прохода насквозь
- [x] 3.3 `tests/player-movement.ts`: проверку «засыпанный персонаж
      выбирается» переписать под новое поведение — обложенный со всех сторон
      реголитом персонаж уходит в сторону сразу, без ожидания
- [x] 3.4 `tests/material-simulation.ts`: обновить проверку про блокировку
      рыхлого реголита; добавить проверку «идущий сквозь насыпь ведёт
      полость» — сыпучее не входит в хитбокс, а общее число ячеек не меняется
- [x] 3.5 `tests/conveyor.ts`: список непроходимых веществ привести
      в соответствие — проверка «корпус модуля остаётся твёрдым» остаётся,
      постройки игрока по-прежнему проходимы

## 4. Цветовые инварианты

- [x] 4.1 `tests/surface-shading.ts`: критерий «вещество толщи» перевести
      с `MAT_SOLID` на состояние (статичное или сыпучее) и прогнать проверку
      «всякая ступень пещеры темнее всякой ступени твёрдого»
- [x] 4.2 `tests/space-backdrop.ts`: тот же перевод для «все заливки задника
      темнее самой тёмной породы»
- [x] 4.3 Если после расширения множества какая-то из двух проверок падает
      (корпус сепаратора, ленты) — разобрать по существу и доложить: это
      находка в палитре, а не повод сузить критерий обратно

## 5. Прогон

- [x] 5.1 `npm run typecheck` и `npm test` — оба зелёные
- [x] 5.2 `npm run shot` — сравнить снимки и метрики: поверхность мира,
      точка старта и куча под сепаратором на месте, персонаж не провалился
      сквозь мир
- [x] 5.3 Разобрать каждое падение снимков и проверок из пункта 5:
      устаревшее ожидание — обновить, расхождение с спекой — доложить
