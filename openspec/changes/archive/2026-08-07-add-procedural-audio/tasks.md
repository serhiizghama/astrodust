## 1. Конфигурация и сигналы

- [x] 1.1 Добавить в `src/config.ts` секцию `AUDIO`: мастер-громкость `0.35`,
  `contactRadius = 96`, `panMax = 0.7`, `rootHz = 110`, ступени пентатоники
  `[0, 3, 5, 7, 10]`, `maxOneShots = 8`, `lookahead = 0.02`, `maxAhead = 0.25`,
  порог изменения параметра `1/256`
- [x] 1.2 Добавить в `AUDIO` блоки дорожек `dig` (`gain`, полосы помола и
  акцентов, `strikeHz = 12`, `fullRate`) и `dust` (`gain`, `fullMoves = 256`,
  `hzQuiet`, `hzLoud`, времена атаки и спада) — каждый со своим выключателем
- [x] 1.3 Создать `src/audio/signals.ts` с типом `AudioSignals` и функцией
  сброса переиспользуемого объекта (полей ровно столько, сколько в
  `design.md § Решение 2`)
- [x] 1.4 Добавить в `src/world/simulation.ts` счётчики за шаг: число успешных
  сдвигов сыпучего и суммы их координат для центра масс. Обнуляются в начале
  шага, инкрементируются в `tryMove` при успехе. Поведение автомата
  не меняется — проверить, что существующие тесты проходят без правок

## 2. Ядро звука

- [x] 2.1 Создать `src/audio/context.ts`: ленивое создание `AudioContext` по
  первому действию игрока, мастер-гейн → `DynamicsCompressorNode` → выход,
  одна попытка создания и флаг «звука не будет» при отказе
- [x] 2.2 Сгенерировать общий буфер белого шума на 1 секунду через `mulberry32`
  от `WORLD_SEED` по фактическому `ctx.sampleRate`
- [x] 2.3 Реализовать `suspend`/`resume` по `visibilitychange` и мгновенное
  (0.02 с) отключение звука мастер-гейном
- [x] 2.4 Создать `src/audio/soundscape.ts`: реестр дорожек, метод `update(dt,
  signals)`, ранний выход до первого действия игрока
- [x] 2.5 Реализовать курсор часов аудио по `design.md § Решение 3`: нижняя
  граница `currentTime + lookahead`, шаг `FIXED_DT`, сброс при уходе дальше
  `maxAhead`; при возврате фокуса курсор и все сглаженные величины сбрасываются
  к текущему состоянию
- [x] 2.6 Реализовать пул одноразовых голосов: восемь постоянных цепочек
  `фильтр → гейн → панорама`, занятость по `Float64Array` времён окончания,
  без колбэков `onended`
- [x] 2.7 Реализовать общие чистые функции в `src/audio/model.ts`: затухание
  `a(d)`, панорама от игрока в координатах мира, привязка частоты к тональной
  сетке, порог изменения параметра

## 3. Дорожка копания

- [x] 3.1 Создать `src/audio/voices/dig.ts` с моделью `digParams` — чистая
  функция от `AudioSignals`, состояния дорожки и `dt`, без импорта WebAudio
- [x] 3.2 Реализовать помол: сглаженный темп выработки (ячеек в секунду) →
  громкость и частота среза полосового шума в полосе 800 Гц – 4 кГц
- [x] 3.3 Реализовать акценты: ограничение темпа `strikeHz = 12`, срабатывание
  только при ненулевой выработке за окно, полоса 400 Гц – 2.5 кГц, короткий
  тональный призвук из общей сетки по детерминированному индексу
- [x] 3.4 Реализовать драйвер дорожки: вечный источник помола, акценты через
  пул слотов, затухание и панорама от точки копания
- [x] 3.5 Реализовать слияние вместо потери: при отсутствии свободного слота
  энергия акцента прибавляется к помолу

## 4. Дорожка пыли

- [x] 4.1 Создать `src/audio/voices/dust.ts` с моделью: `i = min(1,
  sqrt(powderMoves / fullMoves))`, громкость с затуханием, частота среза
  интерполяцией между `hzQuiet` и `hzLoud`
- [x] 4.2 Реализовать драйвер: один зацикленный источник шума, созданный при
  инициализации и не пересоздаваемый; полоса 2–7 кГц
- [x] 4.3 Реализовать сглаживание через `setTargetAtTime`: быстрая атака
  (0.02 с), медленный спад (0.15 с), сход к тишине без щелчка
- [x] 4.4 Панорамировать дорожку по центру масс движения сыпучего

## 5. Подключение к игре

- [x] 5.1 Добавить в `src/core/input.ts` клавишу `M` (в `GAME_KEYS` и как
  игровое действие) и признак «первое действие игрока состоялось» — истинен
  с первого нажатия клавиши или кнопки мыши и дальше не сбрасывается
- [x] 5.2 Создать `Soundscape` в `src/main.ts` и заполнять снапшот сигналов
  в конце шага, рядом с `input.endStep()`, из возвращаемого значения
  `digger.update` и счётчиков `simulation`
- [x] 5.3 Обрабатывать нажатие `M` — переключение мастер-отключения
- [x] 5.4 Убедиться, что порядок в шаге не нарушен: сигналы собираются после
  того, как копание и симуляция отработали, но до `input.endStep()`

## 6. Проверки без браузера

- [x] 6.1 Добавить в `tests/smoke.ts` проверку затухания: `a(0) = 1`,
  монотонное убывание, `a(d) = 0` при `d >= contactRadius`
- [x] 6.2 Проверить, что панорама считается от персонажа: сдвиг камеры к
  курсору не меняет результат при неподвижном персонаже и источнике
- [x] 6.3 Проверить кривую пыли: монотонность, насыщение на `fullMoves`,
  отсутствие превышения единицы при тысячах сдвигов за шаг
- [x] 6.4 Проверить лимит темпа акцентов: при 33 применениях кисти в секунду
  акцентов не больше `strikeHz` в секунду
- [x] 6.5 Проверить курсор часов: пять шагов, выполненных при одном значении
  `currentTime`, дают пять различных времён с шагом `FIXED_DT`; уход дальше
  `maxAhead` возвращает курсор к настоящему
- [x] 6.6 Проверить лимит голосов: залп событий не оставляет больше
  `maxOneShots` занятых слотов, а лишние события вливаются в непрерывный
  компонент
- [x] 6.7 Проверить детерминированность шума: две генерации с одним зерном
  и одной частотой дискретизации совпадают выборка в выборку
- [x] 6.8 Проверить порог изменения параметра: при постоянном сигнале модель
  не сообщает драйверу новых значений
- [x] 6.9 Проверить, что счётчики симуляции не изменили её поведение: сравнить
  состояние мира после N шагов с эталоном из существующей проверки
- [x] 6.10 Прогнать `npm run typecheck`, `npm test`, `npm run build`

## 7. Приёмка на слух

- [x] 7.1 Проверить разблокировку: до первого нажатия тишина, после — звук;
  события до разблокировки не проигрываются залпом
- [x] 7.2 Проверить копание: удары различимы по темпу, копание в пустоте и по
  недостижимой цели молчит, копание по уже рыхлому материалу не звучит ударом
  по породе
- [x] 7.3 Проверить осыпание: текстура нарастает с обвалом, сходит к тишине
  плавно, улёгшийся мир молчит полностью
- [x] 7.4 Проверить правило вакуума: осыпание дальше 96 пикселей не слышно,
  панорама следует за источником и не качается при движении мыши
- [x] 7.5 Проверить одновременное звучание: копание поверх осыпания —
  обе дорожки различимы, перегрузки нет
- [x] 7.6 Прослушать каждую дорожку в одиночку, отключив остальные
  выключателями в конфиге
- [x] 7.7 Проверить утомляемость: две минуты непрерывного копания подряд;
  если звук становится невыносим, поднять вариативность акцентов
- [x] 7.8 Сравнить пресеты «пыль» и «снег» из `design.md § Решение 12` и
  зафиксировать выбор в конфиге
- [x] 7.9 Проверить свёртывание вкладки: тишина при потере фокуса, при
  возврате звук соответствует текущему состоянию мира без залпа
- [x] 7.10 Проверить отключение по `M`: мгновенно, без щелчка, включение
  возвращает текущее состояние мира
- [x] 7.11 Замерить FPS по F3 в сцене обвала со звуком и без: разницы быть
  не должно

## 8. Хвосты

- [x] 8.1 Проверить, что игра работает при удалённой папке `src/audio/`
  и снятом вызове из `main.ts` — направление зависимости соблюдено
- [x] 8.2 Записать в `design.md § Risks` фактическое значение `AUDIO.dig.fullRate`,
  подобранное на слух, и пометку, что после `add-dig-yield-and-preview`
  его нужно перенастроить
- [x] 8.3 Прогнать `npm run format`
