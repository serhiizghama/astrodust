## 1. Модуль 6

- [x] 1.1 `config.ts` — `BUILD_MODULE` 4 → 6; переписать вывод числа: степени
      двойки больше нет, вместо неё названы два предела (ниже персонажа,
      меньше просвета под окном машины)
- [x] 1.2 `config.ts` — `CONVEYOR.rollerInset` 1 → 2: ролики занимают середину
      шестёрки, а не четыре ряда из шести
- [x] 1.3 `render/renderer.ts` — убрать `MODULE_MASK`, взять `wy % BUILD_MODULE`
      там же, в построчной области цикла; убедиться, что на пиксель проверка
      не переехала
- [x] 1.4 `npm run typecheck` — увидеть ровно то, что сломалось от числа

## 2. Глубина тяги в таблице веществ

- [x] 2.1 `world/materials.ts` — поле `carryDepth` рядом с `carry`, ноль
      у всех кроме несущих; у обоих конвейеров — половина модуля из `config.ts`
- [x] 2.2 `world/materials.ts` — развёрнутая таблица `MAT_CARRY_DEPTH`
      и константа `MAX_CARRY_DEPTH` (наибольшая глубина, считается один раз)
- [x] 2.3 `world/index.ts` — вынести обе наружу, если их читает симуляция

## 3. Правило переноса

- [x] 3.1 `world/simulation.ts` — в `movePowder` заменить одиночное чтение
      под грузом спуском по сплошному столбику: выход по пустоте, выход
      по найденной поверхности, сравнение `d ≤ depth`
- [x] 3.2 Проверить, что ветка переноса по-прежнему возвращает управление
      до кода осыпания — запрет диагонали обязан накрыть всю полосу
- [x] 3.3 Проверить, что пустая лента не делает ни одной записи: спуск
      начинается от груза, а не от поверхности
- [x] 3.4 Переписать шапку `movePowder`: схема в комментарии описывает старое
      правило «несёт подо мной?»

## 4. Областной снос

- [x] 4.1 `core/input.ts` — `Alt` как удерживаемый модификатор мышиного жеста;
      в `GAME_KEYS` не добавлять и нажатие с ним игровым не считать
- [x] 4.2 `systems/builder.ts` — `BuildRun.begin` принимает признак области;
      ветка фиксируется в момент нажатия и до конца жеста не меняется
- [x] 4.3 `systems/builder.ts` — `Builder.areaPreview(...)`: прямоугольник
      из двух углов по сетке модуля, обрезанный `Digger.inReach`
- [x] 4.4 `systems/builder.ts` — `Builder.razeArea(...)`: проход по реестру
      пересечением прямоугольников, затем проход по клеткам сетки внутри рамки
      для секционных построек. Постройка, задетая краем, сносится целиком
- [x] 4.5 Снос внутри области идёт теми же `demolish`/`razeSection`: корпус
      в пустоту, накопленное в мир, счёт не трогается
- [x] 4.6 `main.ts` — ветка области рядом с протяжкой; на отпускании сносит,
      во время жеста только показывает
- [x] 4.7 Проверить, что порода, сыпучее и корпус посадочного модуля рамкой
      не задеваются

## 5. Вид

- [x] 5.1 `GhostView` — признак того, что контур означает снос области, а не
      постройку
- [x] 5.2 `render/renderer.ts` — штриховой периметр цветом отказа для области;
      контур постройки не трогать
- [x] 5.3 Убедиться, что штрих различим на светлом реголите и в тёмной пещере

## 6. Проверки

- [x] 6.1 `tests/material-simulation.ts` — полоса едет целиком; выше полосы
      обычные правила; разрыв под ячейкой отменяет перенос; диагональ запрещена
      на всех рядах полосы; пустая поверхность бесплатна; вещество сохраняется
- [x] 6.2 `tests/material-simulation.ts` — глубина берётся из таблицы:
      поверхность с другой глубиной тянет другое число рядов без правки автомата
- [x] 6.3 `tests/conveyor.ts` — секция равна модулю и строго ниже персонажа
      и просвета; лента везёт полосу, а не ряд; ролики по-прежнему повторяются
      с шагом секции
- [x] 6.4 `tests/building-placement.ts` — область сносит все постройки внутри;
      порода и груз целы; вид каталога не влияет; задетая краем сносится целиком;
      накопленное возвращается; пустая рамка ничего не меняет; обрезка дальностью
- [x] 6.5 `tests/game-shell.ts` — `Alt` читается как удержание и не отбирается
      у браузера; ветка жеста зафиксирована в момент нажатия
- [x] 6.6 `tests/separator.ts` — геометрия машины не менялась, но кратность
      модулю проверить явно: 24 и 12 кратны 6
- [x] 6.7 `npm run typecheck` и `npm test` — зелёные

## 7. Замеры и осмотр

- [x] 7.1 `npm run bench` — сравнить стоимость кадра до и после (опорный прогон
      на HEAD в отдельном worktree)
- [x] 7.2 Замер стоимости шага: `lastCellsVisited` на гружёной ленте до и после
      спуска по столбику
- [x] 7.3 `npm run shot` — пересчитать снимки, сверить числовые метрики
- [ ] 7.4 Живая проверка в `npm run dev`: насыпать на ленту кучу выше полосы
      и увидеть, что едет ровно полполенты; выделить рамкой участок фабрики
      и снести одним жестом

## 8. Спеки

- [x] 8.1 Свести дельты в `openspec/specs/` при архивации
