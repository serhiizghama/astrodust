## MODIFIED Requirements

### Requirement: Отрисовка мира

Система SHALL отрисовывать только видимую камерой область мира, окрашивая каждую
ячейку ступенью тонального набора её материала. Выбор ступени — предмет
способности `surface-shading`; настоящее требование фиксирует лишь то, что цвет
ячейки принадлежит набору её материала и никакому другому.

Ячейки пустоты SHALL отображаться фоном, а не «дыркой». Фон различается по
глубине: пустота выше поверхности — небо, пустота ниже поверхности — интерьер
пещеры.

Небо MUST рисоваться отдельным проходом задника до прохода мира, а проход мира
пиксели неба трогать MUST NOT: за содержимое неба отвечает задник, за
содержимое мира — сетка материалов.

Стоимость отрисовки MUST зависеть от размера окна вывода, а не от размера мира:
увеличение мира не должно замедлять кадр.

Ни один пиксель кадра MUST NOT записываться дважды за кадр: проходы задника и
мира делят область кадра по профилю поверхности, а не рисуют один поверх другого.
Тонирование этого не меняет — ступень выбирается до записи пикселя, а не
накладывается поверх уже записанного.

#### Scenario: Отрисовывается только видимое

- **WHEN** мир 1024×512, а камера показывает область 320×180
- **THEN** обрабатывается около 320×180 ячеек, а не 1024×512

#### Scenario: Пустота выше поверхности показывает задник

- **WHEN** камера смотрит на область над поверхностью, заполненную пустотой
- **THEN** видно небо со звёздами и слоями задника, а не плоская заливка

#### Scenario: Пустота ниже поверхности показывает пещеру

- **WHEN** камера смотрит на пустоту внутри лавовой трубки
- **THEN** видны ступени затенения интерьера пещеры, и задник в них не проступает

#### Scenario: Цвет соответствует материалу

- **WHEN** ячейка содержит реголитовую пыль
- **THEN** соответствующий пиксель окрашен одной из ступеней тонального набора
  реголитовой пыли

#### Scenario: Перерисовки нет

- **WHEN** отрисован один кадр, в котором видны и небо, и мир
- **THEN** каждый пиксель буфера кадра записан ровно один раз

### Requirement: Интерьер пещеры читается как пустота

Интерьер пещеры SHALL выводиться тональным набором из профиля мира. Каждая
ступень набора MUST быть темнее любой ступени любого твёрдого материала мира:
пустота обязана читаться как пустота, а не как ещё одна порода, при любом
уровне затенения.

Ни одна ступень набора пещеры MUST NOT совпадать ни с одним площадным цветом
задника — небом, заливкой любого слоя, свечением полосы галактики. Совпадение
сделало бы измерение задника бессмысленным: инструмент, считающий площадь неба,
считал бы заодно и пещеру.

Тёмный контур персонажа MUST оставаться отличимым и от каждой ступени затенения
пещеры, и от неба. Обе границы существенны: силуэт внутри пещеры держится на
контуре, а в полёте над поверхностью персонаж виден на фоне неба — совпадение
контура с любым из этих двух фонов стирает силуэт ровно в одном из двух мест,
где он нужен. Затенение добавляет ступеней, и требование распространяется
на каждую из них, а не только на базовую.

#### Scenario: Пещера темнее породы

- **WHEN** сравниваются светлейшая ступень интерьера пещеры и самая тёмная
  ступень самого тёмного твёрдого материала
- **THEN** ступень пещеры строго темнее

#### Scenario: Силуэт персонажа держится в пещере

- **WHEN** персонаж находится внутри лавовой трубки на фоне любой ступени
  затенения
- **THEN** цвет тёмного контура не совпадает с этой ступенью, и силуэт отделён
  от фона

#### Scenario: Силуэт персонажа держится в небе

- **WHEN** персонаж поднимается на ранце и виден на фоне неба
- **THEN** тёмный контур спрайта светлее неба, и силуэт отделён от фона

#### Scenario: Пещера не подделывает задник

- **WHEN** каждая ступень набора пещеры сравнивается с небом, заливками слоёв
  задника и свечением полосы галактики
- **THEN** ни одна из них не совпадает ни с одним из этих цветов

### Requirement: Детерминированная генерация лунного уровня

Мир Луны SHALL генерироваться из фиксированного зерна: один и тот же вход
всегда даёт один и тот же уровень.

Уровень MUST содержать:
- холмистую лунную поверхность с несколькими кратерами,
- слой реголитовой пыли поверх породы,
- проходимую горизонтальную пещеру (лавовую трубку) под поверхностью,
- связный путь: с точки старта персонаж может дойти до пещеры и вернуться,
  не проходя сквозь породу,
- залежи льда — в толще под днищами кратеров и у стен лавовой трубки,
- **посадочный модуль** на выровненной площадке рядом с точкой старта,
- **слоистость породы по глубине**: границы между породами идут волной,
  а не прямой линией,
- **вкрапления**: редкие включения одной породы в толще другой.

Толща MUST NOT быть однородной. Однородный массив выглядит заливкой независимо
от того, как он затенён: тонирование добавляет зерно в пределах одного вещества,
а разнородность самого вещества даёт крупный рисунок, которого зерном
не получить. Это свойство МИРА, а не кадра: слой и вкрапление видны в коллизии,
имеют свою выработку и выкапываются.

Граница между двумя породами SHALL быть размыта чередованием их ячеек, а не
проведена ровно. Размытие MUST выполняться в самом мире — граница, размытая
только в кадре, была бы враньём о геометрии: игрок видел бы переход там, где
копание встречает ступеньку.

Залежи MUST быть достижимы игровыми средствами с точки старта: хотя бы одна
из них SHALL выходить в объём лавовой трубки — то есть иметь ячейку льда,
граничащую с пустотой пещеры. Залежь, до которой нельзя докопаться,
не отличается от её отсутствия, а «где-то в толще» — это не достижимость,
а надежда на то, что игрок будет копать наугад в нужном месте.

Залежи, слои и вкрапления MUST NOT нарушать связность уровня: проходимый путь
до пещеры и обратно остаётся тем же, что и без них. Достигается это порядком
построения — включения выкладываются до прорезания тоннелей, а не после, —
но требование относится к результату, а не к способу.

Лёд MUST NOT выкладываться в пустоту: залежь — включение в толще, а не парящая
глыба. То же относится к вкраплениям породы. Точка старта персонажа MUST
оставаться на твёрдой поверхности, вне льда и вне корпуса модуля.

Начальный мир MUST NOT содержать ни одной жидкой ячейки. Вода в нём появляется
только как выработка копания: тогда расчёт уровня жидкости в нетронутой партии
не выполняется вовсе, а не выполняется впустую.

#### Scenario: Повторяемость генерации

- **WHEN** уровень сгенерирован дважды с одним и тем же зерном
- **THEN** обе сетки идентичны ячейка в ячейку

#### Scenario: Корректная точка старта

- **WHEN** уровень сгенерирован и персонаж помещён в точку старта
- **THEN** его хитбокс не пересекается с твёрдыми ячейками, и под ним есть опора

#### Scenario: Пещера достижима

- **WHEN** игрок ведёт персонажа от точки старта вглубь
- **THEN** существует проходимый маршрут в лавовую трубку без прохождения сквозь твёрдое

#### Scenario: Лёд в мире есть

- **WHEN** уровень сгенерирован
- **THEN** в нём присутствуют ячейки льда, и они собраны в залежи,
  а не рассыпаны по одной

#### Scenario: Залежь видна с существующего маршрута

- **WHEN** игрок проходит по лавовой трубке
- **THEN** хотя бы одна ячейка льда граничит с её пустотой: залежь видна
  и достижима кистью без прокапывания туннеля через всю толщу

#### Scenario: Залежи не перекрывают маршрут

- **WHEN** уровень сгенерирован с залежами льда
- **THEN** маршрут от точки старта в лавовую трубку и обратно остаётся проходимым

#### Scenario: Модуль есть и стоит ровно

- **WHEN** уровень сгенерирован
- **THEN** рядом с точкой старта присутствует корпус модуля, площадка под ним
  горизонтальна, а зона приёмника открыта сверху

#### Scenario: В нетронутом мире воды нет

- **WHEN** уровень только что сгенерирован
- **THEN** жидких ячеек в нём ноль

#### Scenario: Граница пород идёт волной

- **WHEN** для каждой колонки мира находится верхняя ячейка глубинной породы
- **THEN** эти высоты не одинаковы, и их разброс превышает несколько ячеек

#### Scenario: Вкрапления есть и лежат в толще

- **WHEN** уровень сгенерирован
- **THEN** в толще одной породы присутствуют включения другой, и ни одно из них
  не граничит с пустотой всеми своими ячейками

#### Scenario: Граница пород размыта в мире

- **WHEN** рассматривается полоса ячеек вдоль границы двух пород
- **THEN** в ней встречаются ячейки обеих пород вперемешку, а не ровный стык

#### Scenario: Разнородность не рвёт связность

- **WHEN** уровень сгенерирован со слоями и вкраплениями
- **THEN** маршрут от точки старта в лавовую трубку и обратно остаётся проходимым
