## 1. Измерение переводится на сетку мира

Картинка на этом этапе не меняется ни на пиксель — меняется только то, откуда
проверки берут числа. Шаг обязан быть первым: пока измерение идёт по цвету,
любая ступень роняет тесты по причине, не связанной с тем, что они проверяют.

- [x] 1.1 В `tests/shot.ts` завести подсчёт материала по `world.cells` в видимой
      камерой области и перевести на него проверку сепаратора
      (`countColor(MATERIALS[MAT.IRIDIUM].color)` и то же для шлака)
- [x] 1.2 Проверку «иридий и шлак видны в кадре» оставить по кадру, но свести
      к факту присутствия цвета, а не к точному числу пикселей
- [x] 1.3 В `tests/conveyor.ts` оставить побайтовую сверку полосы: полоса
      и корпус конвейера остаются плоскими, и это отдельный инвариант, а не
      недоделка — его держит проверка `штрих + корпус === 130`
- [x] 1.4 `npm test` зелёный, `npm run shot` даёт те же числовые метрики, что
      и до правки (сверено: подсчёт по сетке и по пикселям дал 9 и 36)

## 2. Общая инфраструктура

- [x] 2.1 Создать `src/render/dither.ts` с матрицей Байера 4×4, перевести на неё
      `backdrop.ts`, убрать приватную копию `BAYER`
- [x] 2.2 Завести в `config.ts` раздел тонирования: число ступеней на материал,
      размер плитки зерна, амплитуды зерна и экспозиции, разрешение карты
      освещённости, радиус её окна, потолок пересчёта на кадр
- [x] 2.3 `npm test` зелёный, кадр не изменился (метрики `npm run shot`
      совпали до числа)

## 3. Таблица ступеней при одной ступени на материал

Чистая замена представления: таблица уже прямоугольная, но заполнена базовым
цветом. Кадр обязан остаться прежним — это и есть проверка того, что конвейер
подключён правильно.

- [x] 3.1 В `material-colors.ts` заменить `MAT_R/MAT_G/MAT_B` прямоугольной
      таблицей ступеней, индексируемой `(material << SHADE_BITS) | shade`;
      число ступеней — степень двойки
- [x] 3.2 Завести тональный набор интерьера пещеры в профиле мира рядом
      с `caveColor` (`caveDeepColor`, `blue[0]`)
- [x] 3.3 Перевести `drawWorld` на выборку по индексу ступени; на этом шаге
      индекс всегда базовый
- [x] 3.4 Опубликовать набор материала и матрицу дизеринга из `render/index.ts`
- [x] 3.5 `npm test` зелёный; `npm run shot` даёт побайтово те же кадры
      (сверено по md5); `npm run bench` не хуже прежнего

## 4. Назначение ступеней и его инварианты

- [x] 4.1 Подобрать акцентные ступени для четырёх пород земляной лестницы
      и для интерьера пещеры. Взяты соседние ступени земляной лестницы плюс
      `blue[0]` для глубины пещеры; у глубинной породы тёмного акцента нет —
      между пещерой (42) и `earth[0]` (54) свободной ступени в палитре нет
- [x] 4.2 Создать `tests/surface-shading.ts` и проверить в нём: базовые ступени
      всех материалов попарно различны; ни одна ступень материала не совпадает
      с небом, заливкой слоя, свечением полосы, ступенью пещеры или цветом
      орбитального объекта; наборы задника и пещеры попарно не пересекаются
- [x] 4.3 Проверить, что каждая ступень пещеры темнее каждой ступени каждого
      твёрдого материала, а контур персонажа не совпадает ни с одной ступенью
      пещеры
- [x] 4.4 Дополнить `tests/space-backdrop.ts` проверкой попарной
      непересекаемости площадных наборов задника — существующая проверка знала
      только `caveColor` и пропустила бы вторую ступень молча

## 5. Зерно, экспозиция и глубина

- [x] 5.1 Плитка зерна 128×128, заполняется один раз через `mulberry32`
      со смещённым распределением; индексируется мировой координатой
- [x] 5.2 Экспозиция: число пустых соседей из четырёх, считается в кадре.
      Соседи по горизонтали не читаются повторно — катятся по строке; края мира
      дают «твёрдая» обнулением смещения и чтением за массив
- [x] 5.3 Глубина под поверхностью из `surface[wx] - wy` как вклад в ступень
- [x] 5.4 Сложить три вклада в один индекс с зажимом в диапазон набора.
      Глубина применяется ДИЗЕРИНГОМ, а не сдвигом ступени: сдвиг опускал бы
      всё распределение зерна и отнимал у базовой ступени доминанту
- [x] 5.5 Затенение интерьера пещеры по глубине (близость к породе — за картой
      освещённости, задача 6.3)
- [x] 5.6 Проверки в `tests/surface-shading.ts`: кадр воспроизводим побайтово;
      текстура не смещается при сдвиге камеры туда-обратно; кромка светлее
      толщи; свежевыкопанная полость немедленно проявляет светлые стенки;
      базовая ступень доминирует в сплошной области; дальний конец полости
      темнее выхода
- [x] 5.7 `npm run shot`: пересмотрены глазами. Добавлена постоянная точка
      съёмки `zoom-cave` ×8 — при ×3 зерно и дизеринг сливаются в тон, и судить
      о них нельзя
- [x] 5.8 `npm run bench`: 0.217 / 0.313 / 0.461 против 0.199 / 0.267 / 0.231 —
      ×1.09 / ×1.17 / ×1.99, в пределах удвоенного

## 6. Карта освещённости

- [x] 6.1 Добавить в `ChunkGrid` перечень чанков, изменившихся с момента
      последнего пересчёта карты; существующие поколения `current`/`next`
      не трогать
- [x] 6.2 Новый модуль карты освещённости в `render/`: разрешение 1 к 4,
      `Uint8Array`, ячейка считается сканом окна вокруг себя
- [~] 6.3 Затенение у стен по плотности твёрдого в окне — **сделано и снято**.
      Внутри сплошной породы плотность окна постоянна, поэтому вклад давал всей
      толще один и тот же сдвиг: не объём, а третий независимый дизеринг поверх
      зерна и глубины. На снимке читался шумом, стоил кадру и не нёс сведений,
      которых нет в экспозиции и глубине. Причина записана в шапке
      `render/lightmap.ts`, чтобы приём не завели заново
- [x] 6.4 Добавить светимость в таблицу веществ, назначить лаве и корпусу
      модуля; вклад по сумме светимостей в окне
- [x] 6.5 Пересчёт грязных чанков с полем шириной в радиус окна, с потолком
      на кадр и переносом остатка
- [x] 6.6 Чтение карты в `drawWorld` с дизерингом между соседними уровнями
- [x] 6.7 Проверки: одна и та же конфигурация мира, полученная копанием
      и генерацией, даёт одинаковую карту; при неизменном мире пересчёта нет;
      порода вокруг лавы светлее такой же породы вдали; свет монотонно убывает
      с расстоянием; новое вещество со светимостью освещает без правок рендера
- [x] 6.8 Проверка потолка: изменение области, многократно превышающей выемку
      копания, распределяется по кадрам (12 чанков за кадр, догон за 6 кадров)
- [x] 6.9 `npm run bench`: 0.229 / 0.335 / 0.506 мс. Требование бюджета
      переписано с отношения к плоской заливке на АБСОЛЮТНЫЙ предел в 1 мс:
      отношение к предыдущей реализации запрещает отрисовке когда-либо стать
      богаче, а его база уплывает с каждой правкой. Причина — в спеке

## 7. Разнородность толщи в генераторе

- [x] 7.1 Слоистость: граница пород по глубине идёт волной через `makeNoise` —
      **уже была в генераторе**, `deepBoundary`. Proposal записал её как новую
      работу зря; добавлена только проверка
- [x] 7.2 Вкрапления: редкие включения одной породы в толще другой —
      **уже были**, валуны `ROCK_DEEP` в `ROCK`. Добавлена только проверка
- [x] 7.3 Размытие границы двух пород чередованием их ячеек в самом мире —
      это и оказалось единственной недостающей частью. Полуширина СВОЯ у каждой
      границы: общая в 6 ячеек шире слоя пыли (5) и пробивала крышу наружу —
      поймано собственной проверкой
- [x] 7.4 Порядок построения сохранён: включения выкладываются до прорезания
      тоннелей
- [x] 7.5 Проверки в `tests/pixel-world.ts`: повторяемость генерации от зерна;
      разброс высот границы глубинной породы превышает несколько ячеек;
      вкрапления есть и лежат в толще; полоса вдоль границы содержит ячейки
      обеих пород вперемешку; породы на поверхности нет; маршрут от старта
      в лавовую трубку и обратно остаётся проходимым; жидких ячеек ноль

## 8. Приёмка

- [x] 8.1 `npm run typecheck` и `npm test` зелёные — 552 проверки.
      `npm run build` собирается
- [x] 8.2 `tests/architecture.ts` зелёный: 53 модуля, 191 ребро, циклов нет.
      Тест лез глубоким импортом в `render/sprites/player` — палитра спрайта
      выведена в `render/index.ts` как публичная ради инварианта контура
- [x] 8.3 `npm run bench`: 0.230 / 0.335 / 0.509 мс при потолке 1 мс
- [x] 8.4 `npm run shot` пересмотрен глазами. Зерно переделано с независимого
      выбора на пиксель на решение по сдвоенной клетке: рябь на пиксель
      читалась помехами приёма, а не породой
- [x] 8.5 Откат проверен временным обнулением `grainAmplitude`, `exposureStep`,
      `depthDitherMax` и светимости: все 50 449 твёрдых пикселей вернулись
      к базовому цвету материала, правок кода не потребовалось
- [x] 8.6 Комментарии пройдены. Удалены мёртвая `grainAt()` и публичность
      `SHADE_R/G/B`, `threshold`, `DITHER_SIZE` — наружу они не выходят,
      а `index.ts` описывает публичную поверхность
