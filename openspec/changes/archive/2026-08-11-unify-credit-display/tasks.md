## 1. Общий вид денежной суммы

- [x] 1.1 Перенести просвет между значком и числом из `HUD.counterGap` в `UI`
      (`src/config.ts`), удалить `counterGap` из `HUD`
- [x] 1.2 Завести `src/render/credits.ts`: `CREDITS_TONE`, `measureCredits`,
      `drawCredits(ui, amount, x, y, style)` — значок монеты, просвет, число;
      выравнивание `left/center/right` по всей группе, возврат ширины
- [x] 1.3 Вертикальное положение значка считать из кегля строки и высоты
      спрайта, а не подставлять числом по месту

## 2. Счётчик в углу кадра

- [x] 2.1 Переписать `drawCredits` в `src/render/hud.ts` через общий вид:
      убрать локальный `CREDITS_COLOR`, импорт `COIN_ICON`/`CURRENCY_ICON`
      и ручное измерение
- [x] 2.2 Убедиться, что счётчик остаётся с ореолом, прижат к правому краю
      и стоит на прежнем месте относительно угла

## 3. Оверлей исследований

- [x] 3.1 Счёт в заголовке (`src/render/overlay.ts`) — общим видом
      заголовочным кеглем, без знака `₡`
- [x] 3.2 Цена под узлом — общим видом мелким кеглем, тон по-прежнему от
      состояния покупки; ширину суммы учитывать при подгонке под шаг колонки
- [x] 3.3 Цена в полосе сведений — общим видом кеглем полосы, выравнивание
      по правому краю панели
- [x] 3.4 Проверить, что подпись «открыта» осталась текстом: это состояние,
      а не сумма

## 4. Причина недоступности — значением, а не строкой

- [x] 4.1 Ввести `TechNote` и переписать `statusNote` в
      `src/progress/research.ts`: возвращает `none` / `short` с суммой /
      `locked` со списком предпосылок; знака `₡` в модуле не остаётся
- [x] 4.2 Обновить публичную поверхность `src/progress/index.ts`
- [x] 4.3 Изменить тип `OverlayNode.note` на `TechNote` и передавать значение
      из `src/main.ts` без форматирования
- [x] 4.4 Собирать строку причины в полосе сведений: слова + сумма общим
      видом; обрезается по месту слово, а не сумма

## 5. Проверки

- [x] 5.1 `tests/ui-layer.ts`: сумма — значок и число одним тоном и в одном
      порядке; ширина измеряется; выравнивание по правому краю держится при
      смене числа знаков
- [x] 5.2 `tests/ui-layer.ts`: ни одна надпись кадра не содержит знака `₡`
      (кадр с открытым оверлеем и видимой панелью)
- [x] 5.3 `tests/game-hud.ts`: счётчик рисуется тем же значком, что и суммы
      оверлея
- [x] 5.4 `tests/research.ts`: у каждого некупленного узла сумма нарисована
      значком и числом; тон цены различается по состоянию; недостающая сумма
      в полосе сведений показана тем же видом
- [x] 5.5 `tests/research.ts`: проверки `statusNote` переведены на `TechNote`
- [x] 5.6 Прогнать `npm run typecheck` и `npm test`

## 6. Внешний вид

- [x] 6.1 Пересмотреть снимки `npm run shot:ui`: счёт, цены под узлами и
      полоса сведений обозначены одинаково
- [x] 6.2 Сверить стоимость кадра интерфейса с прежним замером: лишние значки
      не должны утаскивать кадр за шаг
- [x] 6.3 Обновить шапки затронутых модулей: убрать из `hud.ts` и `overlay.ts`
      упоминания собственного обозначения валюты
