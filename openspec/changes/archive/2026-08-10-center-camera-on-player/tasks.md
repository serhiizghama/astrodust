## 1. Удаление смещения кадра

- [x] 1.1 `src/render/camera.ts`: свести `follow()` к `follow(targetX, targetY)` —
      убрать параметры смещения, прибавление `desiredX += lookAheadX` /
      `desiredY += lookAheadY` и упоминание взгляда мыши в комментариях класса
- [x] 1.2 `src/app/game.ts`: убрать `lookAheadX`/`lookAheadY` из `StepIntent`
      и передавать в `camera.follow()` только центр персонажа
- [x] 1.3 `src/main.ts`: удалить вычисление смещения в `worldState.handleInput`
      (ветку `mouseAim ? ... : dir.x * CAMERA.keyLookAhead` по обеим осям)
      и соответствующий комментарий про «взгляд игрока»
- [x] 1.4 `src/main.ts`: удалить `clamp()` — после 1.3 он остаётся без
      пользователей; проверить, не остались ли без пользователей импорты
      `VIEW_W`/`VIEW_H`/`CAMERA`, и убрать неиспользуемые
- [x] 1.5 `src/config.ts`: удалить `CAMERA.mouseLookAheadMax`,
      `CAMERA.mouseLookAheadFactor`, `CAMERA.keyLookAhead` вместе
      с их комментариями; `deadzoneHalfW/H` и `smoothing` оставить
- [x] 1.6 `npm run typecheck` — убедиться, что ни одного пользователя удалённого
      не осталось

## 2. Разворот персонажа по курсору

- [x] 2.1 `src/app/game.ts`: добавить в `StepIntent` поле `faceX: -1 | 0 | 1`
      («не разворачивать» — ноль) и описать его контракт
- [x] 2.2 `src/app/game.ts`: в `advanceWorld` применять `faceX` к `player.facing`
      СРАЗУ ПОСЛЕ `player.update` — так курсор перекрывает ось движения;
      порядок остальных обновлений не трогать
- [x] 2.3 `src/core/input.ts`: `cursorSide()` — сторона курсора относительно
      центра персонажа с полосой нечувствительности в половину хитбокса
      (в `main.ts` она была бы непроверяема); `src/main.ts`: звать её
      в `worldState.handleInput` только при `input.aimSource === 'mouse'`,
      иначе `faceX` — ноль
- [x] 2.4 `src/main.ts`: в `overlayState.handleInput` вернуть `faceX: 0` —
      при открытом меню персонаж разворотов не получает, как и остального ввода

## 3. Проверки

- [x] 3.1 `tests/game-shell.ts`: обновить вызовы `cam.follow()` под новую
      сигнатуру; заменить проверку «Камера: смещается в сторону курсора»
      на проверку, что у неподвижной цели кадр не меняется за много шагов
- [x] 3.2 `tests/game-shell.ts`: обновить литералы `StepIntent`
      (`lookAheadX`/`lookAheadY` → `faceX`) в проверках порядка шага
- [x] 3.3 `tests/game-shell.ts`: добавить проверку разворота — при активном
      источнике «мышь» курсор слева от персонажа даёт `facing === -1` даже
      при движении вправо, при клавиатурном источнике `facing` идёт от оси
      движения, а курсор в пределах ширины персонажа разворот не меняет
- [x] 3.4 `tests/procedural-audio.ts`: перестроить проверку панорамы — расхождение
      слушателя и кадра создавать уходом цели за мёртвую зону, а не смещением
      к курсору; утверждение «панорама не изменилась» сохранить
- [x] 3.5 `npm run typecheck` и `npm test` — зелено

## 4. Проверка вживую и закрытие

- [x] 4.1 `npm run shot` — убедиться, что сцены снимаются и метрики не разъехались
- [x] 4.2 Живая проверка в `npm run dev`: персонаж стоит, курсор водится по всему
      экрану — кадр неподвижен; персонаж бежит — кадр следует; курсор слева —
      спрайт смотрит влево
- [x] 4.3 Прогнать прохождение без мыши: после загрузки страницы не касаться
      мыши — разворот и клавиатурный прицел идут от движения
