## 1. Кадр становится рантайм-величиной

- [x] 1.1 В `config.ts`: `VIEW_W`/`VIEW_H` → `BASE_VIEW_W`/`BASE_VIEW_H` = 640/360,
      добавить `MAX_VIEW_W`/`MAX_VIEW_H` = 768/432. В шапке — вывод обоих чисел
      (опорный кадр вдвое от прежнего; потолок держит стоимость кадра и объём
      мира в кадре)
- [x] 1.2 `core/display.ts`: множитель и размер буфера по правилу из `design.md`
      (`round` от вписывания в опорный кадр, повышение до укладки в потолок,
      `ceil` от деления окна на множитель). `ImageData` пересоздаётся при
      изменении размера, альфа заполняется заново, наружу — `width`, `height`,
      `scale` и колбэк об изменении
- [x] 1.3 `index.html`: канвас от левого верхнего угла, `overflow: hidden`
      на `body`, центрирование убрать. Комментарий про цвет леттербокса —
      удалить вместе с самим леттербоксом
- [x] 1.4 `render/camera.ts`: размеры кадра параметром (`setViewport`),
      мёртвая зона и ограничение границами мира — от них
- [x] 1.5 `render/backdrop.ts`: ширина и высота кадра — параметры `draw`
      и `maxSurfaceInView`; `skyFloor` предвыделяется на `MAX_VIEW_W`
- [x] 1.6 `render/renderer.ts`: размеры кадра из `Display` в локальные
      переменные в начале каждого метода; строка состояния и счётчики —
      от рантайм-высоты и ширины
- [x] 1.7 `render/overlay.ts`: размеры панели от размеров кадра, переданных
      аргументом
- [x] 1.8 `core/input.ts`: ограничение курсора размерами буфера из `Display`
- [x] 1.9 `main.ts`: связать `Display` → `Camera.setViewport` при старте
      и на изменении окна
- [x] 1.10 `npm run typecheck` проходит

## 2. Пересчёт величин по трём семействам

- [x] 2.1 Длины ×2 в `config.ts` по таблице `design.md` (движение, копание,
      сбор, камера, звуковой радиус, тонирование, задник, машины)
- [x] 2.2 `LUNA.gravity` 320 → 640 и `milkyWay.centerY`/`halfWidth` 48/28 →
      96/56 в `world/worlds/luna.ts`
- [x] 2.3 Объёмы и цены ×4: `VACUUM.capacity`, `SEPARATOR.cost`,
      `CONVEYOR.sectionCost`, пороги звука, цены в `progress/technologies.ts`
- [x] 2.4 `SEPARATOR`: `batch` 5 → 10, `delaySec` 1 → 0.5, `bufferBatches`
      4 → 8, иридия на порцию 1 → 2 (доля выработки на ячейку пульпы — прежняя;
      если число иридия зашито в `entities/separator.ts`, вынести константой)
- [x] 2.5 Проверить инварианты, записанные в комментариях `config.ts`, на новых
      числах: `airAccelThrust < groundAccel`, `maxRiseSpeed < jumpVelocity`,
      `VACUUM.radius < DIG.radius`, `DIG.aimDistance ≤ hitboxW/2 + DIG.radius`,
      `SEPARATOR.width`/`window` нечётные, `batch ≤ window - 2`,
      `contactRadius = DIG.reach × 2`, `lightScale` делит `CHUNK_SIZE`
- [x] 2.6 Соразмерное персонажу оставить нетронутым и дописать в шапку `PLAYER`
      и `MODULE`, почему эти числа не удваиваются

## 3. Мир 2048×1024

- [x] 3.1 `WORLD_W`/`WORLD_H` → 2048/1024
- [x] 3.2 `world/worlds/luna.ts`: ориентиры ×2 (`SURFACE_BASE`, `DUST_DEPTH`,
      `DEEP_ROCK_Y`, `TUBE_FROM_X`, `TUBE_TO_X`, `TUBE_BASE_Y`, `TUBE_WOBBLE_Y`,
      `ICE_ANCHOR_X`, начало спуска, точка старта, `MODULE.x`)
- [x] 3.3 Амплитуды шума поверхности: крупная 20 → 40, средняя 7 → 14, мелкая
      остаётся 2 (иначе рябь перерастает автоподъём — вывод записать в код)
- [x] 3.4 Счётчики: валуны 90 → 360, рассеянные залежи 8 → 32 (площадь),
      кратеры 6 → 12 (линия поверхности); размеры валунов и линз ×2
- [x] 3.5 Полосы залегания (`by` валунов и залежей) пересчитать от новой
      глубины, а не удваивать вслепую
- [x] 3.6 `npm test` — набор `pixel-world` проходит, включая связность маршрута
      от старта в лавовую трубку и обратно

## 4. Проверки

- [x] 4.1 `tests/game-shell.ts`: сценарии нового требования — полос нет при
      окне, не кратном множителю; окно 1920×1080 даёт ровно опорный кадр;
      потолок буфера соблюдается; при изменении окна кадр по-прежнему покрывает
      его целиком
- [x] 4.2 Правило подбора множителя вынести так, чтобы оно проверялось без
      браузера (чистая функция «окно → множитель и размер кадра»)
- [x] 4.3 Остальные 10 наборов: `VIEW_W`/`VIEW_H` → `BASE_VIEW_W`/`BASE_VIEW_H`,
      рендер в буфер опорного размера, размеры кадра передаются явно
- [x] 4.4 `tests/space-backdrop.ts`: сценарии нового требования — звёзды доходят
      до верха кадра при камере у верхней границы мира; полоса млечного пути
      целиком внутри окна неба
- [x] 4.5 Числа, завязанные на размер мира и кадра, в наборах `research`,
      `procedural-audio`, `conveyor`, `separator`, `player-inventory`,
      `terrain-digging` — пересчитать, соотношения сохранить
- [x] 4.6 `tests/architecture.ts` проходит: новых импортов вверх по слоям нет
- [x] 4.7 `npm run typecheck` и `npm test` — зелёные

## 5. Замеры и вид

- [x] 5.1 `npm run bench`: стоимость кадра на опорном 640×360 и на потолке
      768×432. Ожидание — около 2.0 и 2.9 мс; при заметном расхождении
      разобраться до продолжения
- [x] 5.2 Замерить время генерации мира и первых шагов после `wakeAll()`;
      при заметной задержке старта будить только чанки с сыпучим
- [x] 5.3 `npm run shot`: обновить эталонные скриншоты, сверить, что экранные
      габариты персонажа, кисти, машин и модуля близки к прежним
- [x] 5.4 `npm run verify:audio` — радиусы и пороги не сломали звуковую картину
- [x] 5.5 Прогон в браузере: при окне 1710×951 канвас 1710×951 (буфер 570×317
      ×3), при 1900×1056 — 1902×1056 (буфер 634×352 ×3), полос нет ни в одном.
      Ощущение бега и прыжка глазами НЕ проверено: вкладка под управлением
      расширения не получает кадров, и игра в ней не идёт
