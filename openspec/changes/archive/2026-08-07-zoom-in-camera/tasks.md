## 1. Снимки «до»

- [x] 1.1 Снять эталон до изменений: `npm run shot before` — в `shots/`
      появятся `*-before.png`. Без них сравнивать композицию будет не с чем.
- [x] 1.2 Записать текущие числа `npm run bench` в заметку, чтобы после
      изменения не спутать выигрыш от меньшей площади кадра с оптимизацией.
      Снято при 480×270: небо 0.373, поверхность 0.374, трубка 0.381 мс/кадр.

## 2. Разрешение кадра

- [x] 2.1 `src/config.ts`: `VIEW_W = 320`, `VIEW_H = 180`. В комментарии
      к ним объяснить, почему ×1.5, а не мягче (целый множитель шагает
      не на каждом уменьшении — см. design.md, Decision 2).
- [x] 2.2 `src/config.ts`: комментарий к `WORLD_W`/`WORLD_H` — «~2x2 экрана»
      больше не верно, теперь ~3.2×2.8.
- [x] 2.3 `index.html`: в комментарии к `#game` разрешение 480x270 → 320x180.
- [x] 2.4 Проверить, что всё собирается и тесты проходят на одном лишь этом
      шаге: `npm run typecheck && npm test`. Картинка на этом шаге ещё сломана
      (задник утонул) — это ожидаемо, чинится в блоке 4.

## 3. Камера

- [x] 3.1 `src/config.ts`, блок `CAMERA`: `deadzoneHalfW` 40 → 27,
      `deadzoneHalfH` 24 → 16 (÷1.5 — мёртвая зона задана долей кадра).
- [x] 3.2 `src/config.ts`, блок `CAMERA`: `mouseLookAheadMax` 32 → 21,
      `keyLookAhead` 32 → 21.
- [x] 3.3 Убедиться, что предел смещения к курсору по-прежнему достижим:
      максимум `|mouseX − VIEW_W/2|` = 160, при `mouseLookAheadFactor = 0.25`
      это 40 ≥ 21. `mouseLookAheadFactor` и `smoothing` не трогать.

## 4. Слои задника

- [x] 4.1 `src/world/worlds/luna.ts`, `backdrop.layers`: `crestY`
      126 → 88, 139 → 108, 154 → 134 (по правилу `crestY − 45·(1−p)`,
      design.md Decision 4). В комментарии зафиксировать само правило,
      а не только результат — иначе следующая смена высоты кадра повторит
      ту же ошибку.
- [x] 4.2 Не трогать `amplitude` (16/22/28) и `detail` (40/19/9): они
      в буферных пикселях и уже дают ×1.5 к размеру формы.
- [x] 4.3 Снять `npm run shot after` и глазами проверить `horizon-after.png`
      и `surface-after.png`: все три слоя силуэтов должны читаться над линией
      горизонта, как на `*-before.png`. Это сценарий «Слои видны с поверхности».
      `horizon-after`: слои 1307/8733/15526 — все три на месте (было 828/5925/17086).
      В кадре спавна дальнего слоя нет, но его нет и в кадре «до» в том же окне
      320×180 — это кроп, а не регрессия. Сценарий в дельта-спеке поправлен
      на настоящий инвариант: речь о линии гребня, а не о площади в каждом кадре.

## 5. Небо

- [x] 5.1 `src/config.ts`, блок `BACKDROP`: `starFieldH` 212 → 141
      (видимое окно неба — максимум ~87 строк поля; остальное генерируется
      впустую). `starDensity` в `luna.ts` не трогать: плотность звёзд
      на экране от `starFieldH` не зависит.
- [x] 5.2 `src/world/worlds/luna.ts`, `milkyWay`: `centerY` 70 → 48,
      `halfWidth` 44 → 28. `tilt` (0.06) и `densityBoost` (3.4) оставить.
- [x] 5.3 `src/world/worlds/luna.ts`: `companion` `{x: 372, y: 46}` →
      `{x: 253, y: 32}`. Правка по X обязательна: при 372 диск при любом
      положении камеры оказывается за правым краем кадра 320.
- [x] 5.4 `src/world/worlds/luna.ts`: `orbiter.y` 34 → 25, `crossSec` 13 → 9
      (экранная скорость прохода остаётся прежней). `periodSec` оставить.
- [x] 5.5 `src/config.ts`: `BACKDROP.vertParallaxLimit` (200) и
      `companionSize` (22) оставить как есть — обосновать в комментарии,
      если он станет неточным.

## 6. Комментарии, ставшие ложными

- [x] 6.1 `src/config.ts`, `AUDIO.contactRadius`: значение 96 не меняется,
      но обоснование через полуширину кадра (240) больше не работает — 160.
      Переписать обоснование на `DIG.reach` (96 = вдвое дальше копания),
      сохранив мысль «слышно ближе, чем видно».
- [x] 6.2 `src/world/worlds/luna.ts`, комментарий к `rimWarm`/`rimCold`:
      «при апскейле ×3–×4» — диапазон множителя сместился, поправить.
- [x] 6.3 Пройти `grep -n "480\|270\|240\|135" src index.html` и убедиться,
      что не осталось комментариев, считающих от старого кадра.

## 7. Снимки и тесты

- [x] 7.1 `tests/shot.ts`: вырезка `zoom-earth` `{x:348,y:22,w:70,h:50}`
      выходит за кадр 320 — перенацелить на новое место диска
      (ориентир: `{x:226, y:22, w:44, h:40}`, уточнить по картинке).
- [x] 7.2 `tests/shot.ts`: вырезка `zoom-ridge` `{x:120,y:100,w:180,h:70}`
      формально в границах, но смотрит уже не на гребни — перенацелить
      (ориентир: `{x:80, y:76, w:160, h:66}`, уточнить по картинке).
- [x] 7.3 `npm test` — весь смоук зелёный. Особое внимание к проверкам,
      считающим от `VIEW_W`/`VIEW_H`: упор камеры в границы мира и учёт
      видимых колонок.
- [x] 7.4 `npm run typecheck` и `npm run build` — без ошибок.
- [x] 7.5 `npm run bench` — записать новые числа рядом со снятыми в 1.2,
      отметив, что площадь кадра уменьшилась в 2.25 раза.
      Стало: небо 0.191, поверхность 0.165, трубка 0.160 мс/кадр — падение
      в 2.0–2.4 раза, ровно за счёт площади кадра.

## 8. Приёмка глазами

- [x] 8.1 `npm run dev`, открыть в браузере: персонаж заметно крупнее,
      текстура скафандра и выхлоп ранца читаются, зерно грунта различимо.
      На этом мониторе окно 2133×1064 даёт буфер 320×180 при множителе ×5
      (1600×900); при прежнем 480×270 было бы ×3. Спрайт вырос с 24×36
      до 40×60 CSS-пикселей, чёрных полей стало меньше. Видны забрало,
      светотень скафандра, ботинки.
- [x] 8.2 Пробежать от левого края мира до правого: диск Земли всё время
      целиком в кадре (сценарий «Земля видна с любой точки поверхности»).
      Закрыто тестом вместо ручного пробега: смоук проверяет все положения
      камеры разом — запас слева 218, справа 45, сверху 22 px.
- [x] 8.3 Посмотреть на небо с поверхности: млечный путь читается как
      наклонная полоса, а не как сплошная засветка всего неба.
- [ ] 8.4 Побегать и попрыгать: кадр не дёргается на мелких шагах, смещение
      к прицелу ощущается как раньше — если нет, подстроить числа из блока 3.
      НЕ ПРОВЕРЕНО на ощупь: в фоновой вкладке браузер троттлит rAF, и оценить
      плавность на реальной частоте кадров из автоматизации нельзя. Числа
      выверены арифметически (блок 3), персонаж в снятых кадрах держится
      в центре. Живую проверку оставляю за человеком.
- [x] 8.5 Проверить пиксель: включить F3, убедиться что HUD читается и
      не вылезает за кадр 320×180; края спрайтов чёткие, без размытия.
      HUD — две строки 8px в левом верхнем углу, помещается с запасом.
- [x] 8.6 Сравнить `shots/*-before.png` и `shots/*-after.png` попарно:
      кроме приближения и перетасованных звёзд, композиция должна совпадать.
      Кадр спавна сверен попиксельно: он в точности вырезка кадра «до»
      со сдвигом 45 строк (расхождения только там, где небо намеренно
      пересобрано). `horizon`: все три слоя читаются, как и было.

## 9. Спеки

- [x] 9.1 Прогнать `openspec validate zoom-in-camera --strict`.
- [ ] 9.2 После приёмки — свести дельты в основные спеки
      (`/opsx:archive` или `/opsx:sync`).
