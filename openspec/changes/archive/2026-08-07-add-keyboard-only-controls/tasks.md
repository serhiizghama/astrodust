## 1. Раскладка и снапшот ввода

- [x] 1.1 Добавить `KeyW` и `KeyS` в `GAME_KEYS` в `src/core/input.ts`
- [x] 1.2 Перевести `jumpPressed` / `jumpHeld` с `Space` на `KeyW` и `ArrowUp`;
  убедиться, что `Player` не правится ни в одной строке
- [x] 1.3 Добавить геттер `digHeld` — истинен при удержании `Space` **или**
  левой кнопки мыши
- [x] 1.4 Добавить вертикальную ось прицела `aimAxisY`: `+1` при `S`/`↓`,
  `−1` при `W`/`↑`, `0` при обеих или ни одной (по образцу `moveAxis`)
- [x] 1.5 Добавить признак активного источника прицела `aimSource`
  (`'mouse' | 'keys'`) с начальным значением `'keys'`: переключается движением
  курсора и нажатием его кнопки в `'mouse'`, нажатием клавиши направления или
  копания — в `'keys'`
- [x] 1.6 Запретить переключение `aimSource`, пока `digHeld` истинен

## 2. Клавиатурный прицел

- [x] 2.1 Добавить `DIG.aimDistance = 6` в `src/config.ts` с комментарием,
  откуда взято число (`hitboxW / 2 + DIG.radius`)
- [x] 2.2 Реализовать чистую функцию сборки направления прицела: горизонталь
  из `moveAxis`, вертикаль из `aimAxisY`, при нулевом векторе — `player.facing`
- [x] 2.3 Реализовать чистую функцию вычисления цели: центр персонажа плюс
  нормированное направление × `aimDistance`, с округлением до ячейки
- [x] 2.4 Проверить на диагоналях, что нормирование не даёт цель дальше
  `aimDistance` (диагональ `(1,1)` — это `≈(4.2, 4.2)`, а не `(6, 6)`)

## 3. Подключение в игровом шаге

- [x] 3.1 В `src/main.ts` выбирать цель копания по удерживаемому органу:
  ЛКМ → курсор через `camera.screenToWorld`, иначе клавиша копания →
  клавиатурная цель от персонажа. Мышь выигрывает при удержании обоих
- [x] 3.2 Передавать в `digger.update` `input.digHeld` и выбранную цель;
  сигнатуру `Digger` не менять
- [x] 3.3 Сохранить существующий порядок в шаге (копание → игрок → симуляция);
  `player.facing` читается с предыдущего шага осознанно
- [x] 3.4 Оставить в `renderer.render` координаты курсора: крестик всегда под
  курсором и никуда не переезжает. `targetInReach` считать по курсорной цели —
  цвет крестика относится к курсору
- [x] 3.5 Считать смещение камеры от активного источника управления: для
  `'mouse'` — прежняя формула, для `'keys'` — направление × `CAMERA.keyLookAhead`
- [x] 3.6 Добавить `CAMERA.keyLookAhead` в `src/config.ts`, начальное значение
  равно `mouseLookAheadMax`

## 4. Проверки без браузера

- [x] 4.1 Добавить в `tests/smoke.ts` проверку инварианта
  `DIG.aimDistance <= PLAYER.hitboxW / 2 + DIG.radius`
- [x] 4.2 Проверить отсутствие зазора: для всех восьми направлений кисть,
  применённая к клавиатурной цели, задевает хотя бы одну ячейку, примыкающую
  к хитбоксу, и не оставляет нетронутых ячеек между телом и выемкой
- [x] 4.3 Проверить, что клавиатурная цель всегда в пределах `DIG.reach`
  для всех восьми направлений
- [x] 4.4 Проверить сборку направления: восемь комбинаций клавиш дают восемь
  разных направлений, а пустой ввод даёт направление по `facing`
- [x] 4.5 Проверить взаимное гашение: `A` и `D` вместе, `W` и `S` вместе дают
  ноль по своей оси
- [x] 4.6 Проверить арбитраж источника (он влияет только на камеру): переключение
  по последнему устройству, отсутствие переключения при удержании копания,
  начальное значение `'keys'`
- [x] 4.7 Проверить, что физика прыжка не изменилась: существующие проверки
  прыжка и ранца проходят без правок
- [x] 4.9 Проверить выбор цели копания: при удержании ЛКМ — курсор, при
  удержании только клавиши копания — цель у персонажа, при обоих — курсор
- [x] 4.8 Прогнать `npm run typecheck`, `npm test`, `npm run build`

## 5. Приёмка на ощупь

- [x] 5.1 Пройти всю игру без мыши: бег, прыжок, полёт на ранце, копание во
  всех восьми направлениях, прокоп тоннеля вбок и вниз
- [x] 5.2 Проверить, что `Space` не прыгает, а `W` и `↑` прыгают одинаково
- [x] 5.3 Проверить прокоп вбок: после одного применения кисти в выемку можно
  шагнуть без дополнительного копания
- [x] 5.4 Проверить копание вниз: персонаж проваливается в собственную выемку
- [x] 5.5 Проверить копание вверх: подбрасывание от `W` ощущается как прокоп
  вверх, а не как помеха. Если мешает — подавлять прыжок при удержании копания
  (альтернатива из `design.md § Risks`)
- [x] 5.6 Проверить `D` + `W` + `Space` вместе: бег, прыжок и копание
  вправо-вверх не мешают друг другу
- [x] 5.7 Проверить, что крестик всё время остаётся под курсором: копание мышью,
  затем клавишей, затем бег с клавишами — крестик ни разу не переезжает
  к персонажу
- [x] 5.8 Проверить, что дрожание мыши во время копания с клавиатуры не уводит
  ни цель, ни кадр
- [x] 5.9 Проверить кадр без мыши: после загрузки страницы, не трогая мышь,
  пробежать вправо и влево — кадр смещается по направлению бега, а не к центру
- [x] 5.10 Подобрать `CAMERA.keyLookAhead` на ощупь
- [x] 5.11 Проверить, что `Space` не прокручивает страницу и не активирует
  ничего в браузере

## 6. Хвосты

- [x] 6.1 Обновить упоминания управления в комментариях `input.ts` и `config.ts`
  под новую раскладку
- [x] 6.2 Прогнать `npm run format`
