# Задачи

## 1. Примитивы поверх буфера

- [x] 1.1 Завести `src/render/draw.ts`: `fillRect`, `strokeRect`, `blit`
      спрайта по индексам палитры — все над `Uint8ClampedArray` с шириной
      и высотой параметрами, без `Display`, чтобы проверялись в Node
- [x] 1.2 Перевести `Renderer` на эти примитивы там, где он рисует
      прямоугольники (контур постройки, полоса хода машины), убрав
      дублирующий код

## 2. Растровый шрифт

- [x] 2.1 Завести `src/render/font.ts`: строки глифов `#`/`.` высотой 7,
      разбор в `Uint8Array` при загрузке модуля, ширина глифа из данных,
      шаг = ширина + 1
- [x] 2.2 Набрать глифы: цифры, знаки препинания, `₡`, `✦`, `▸`, `▶`, `/`,
      `·`, латиница обоих регистров
- [x] 2.3 Набрать кириллицу обоих регистров, включая `ё`/`Ё`
- [x] 2.4 Реализовать `textWidth(s)` и `drawText(px, w, h, s, x, y, color, shadow)`;
      тень — соседний пиксель цветом палитры, отключаемая для текста
      на непрозрачной подложке
- [x] 2.5 Заполнитель для символа без глифа — видимый, шириной как обычный глиф
- [x] 2.6 Экспортировать шрифт из `src/render/index.ts`

## 3. Проверки шрифта

- [x] 3.1 Завести `tests/pixel-text.ts` и подключить его в `tests/run.ts`
- [x] 3.2 Проверить: пустая строка не меняет буфер; ширина строки совпадает
      с крайним занятым пикселем; вывод не пишет за границы буфера
- [x] 3.3 Проверить: каждый символ названий веществ, построек, технологий,
      их описаний и статических надписей интерфейса имеет глиф
- [x] 3.4 Проверить: неизвестный символ даёт заполнитель, а хвост строки
      не сдвигается
- [x] 3.5 Проверить: цвет каждого пикселя буквы и её тени принадлежит `RAMP`

## 4. Перевод существующих надписей на шрифт

- [x] 4.1 Перенести `display.present()` в конец `Renderer.render()`, а вывод
      строки состояния, диагностики и оверлея — перед ним; обновить
      комментарий о порядке
- [x] 4.2 Переписать `drawStatus` и `drawDebug` на `drawText` поверх буфера
- [x] 4.3 Переписать `overlay.ts` на `drawText` и пиксельные прямоугольники,
      пересчитав отступы и высоту строки под новый кегль
- [x] 4.4 Убрать `css()` из вызовов рендера и `ctx.font`/`ctx.fillText`
      из проекта целиком; оставить `ctx` только `putImageData`

## 5. Панель действий: состояние

- [x] 5.1 Заменить `ToolModeState` на `ActionBarState` в `src/core/input.ts`:
      таблица из десяти слотов, `select(i)` игнорирует пустой, `cycle()` идёт
      по непустым, `mode` отдаёт режим активного слота
- [x] 5.2 Добавить `Digit1`…`Digit9`, `Digit0` в игровые клавиши
      с `preventDefault` только без модификаторов и геттер выбранного слота
- [x] 5.3 Обновить `src/core/index.ts` и вызовы в `src/main.ts`

## 6. Панель действий: вид и попадание

- [x] 6.1 Добавить размеры панели, слота, значков и отступы счётчиков
      в `src/config.ts`
- [x] 6.2 Завести `src/render/sprites/icons.ts`: значки копания, строительства
      и сбора 12×12, монеты и кристалла 7×7 — строками с индексами палитры
- [x] 6.3 Завести `src/render/hud.ts`: `hudLayout(viewW, viewH)`,
      `slotAtPoint(x, y, layout)` и отрисовка панели — подложка, слоты,
      подписи клавиш, значки, выделение активного и подсветка наведённого
- [x] 6.4 Расширить `HudState`: `slots`, `activeSlot`, `hoveredSlot`;
      убрать `mode` и `machineSummary`
- [x] 6.5 Нарисовать счётчики валют в правом верхнем углу со значками
- [x] 6.6 Нарисовать строку инвентаря над панелью и, в режиме строительства,
      вид постройки с ценой и причину отказа
- [x] 6.7 Экспортировать `hudLayout` и `slotAtPoint` из `src/render/index.ts`

## 7. Связывание в шаге игры

- [x] 7.1 В `main.ts` считать наведённый слот из позиции курсора до применения
      инструментов; при открытом оверлее не считать вовсе
- [x] 7.2 Клик левой кнопкой по слоту выбирает его; мышиное применение
      инструмента при курсоре над панелью до мира не доходит, `Space` работает
      как прежде
- [x] 7.3 Собрать `slots`/`activeSlot`/`hoveredSlot` в `hudState()`, убрать
      сводку по машинам из кадра

## 8. Проверки интерфейса

- [x] 8.1 Завести `tests/game-hud.ts` и подключить его в `tests/run.ts`
- [x] 8.2 Проверить раскладку: десять слотов, панель по центру нижнего края,
      размер слота не зависит от размера буфера, счётчики не пересекаются
      с панелью
- [x] 8.3 Проверить попадание: точка внутри слота даёт его номер, зазор
      и область вне панели дают «мимо»
- [x] 8.4 Проверить состояние: прямой выбор за одно нажатие, пустой слот
      не становится активным, перебор обходит пустые и возвращается к началу
- [x] 8.5 Проверить кадр: активный и наведённый слоты отличаются друг от друга
      и от обычного, панель и строка инвентаря рисуются при выключенной
      диагностике, подписи режима в кадре нет
- [x] 8.6 Дополнить `tests/game-shell.ts` проверкой признака «курсор над
      интерфейсом» и того, что клавиша применения ему не подчиняется
- [x] 8.7 Обновить `tests/player-inventory.ts` под новые требования спеки

## 9. Обвязка и приёмка

- [x] 9.1 Обновить `IDLE_HUD` в `tests/harness.ts`, фикстуры `tests/shot.ts`
      и `tests/bench-render.ts` под новую форму `HudState`
- [x] 9.2 Добавить в `tests/shot.ts` сцену панели действий и сцену счётчиков
- [x] 9.3 Прогнать `npm run typecheck`, `npm test`, `npm run bench`,
      `npm run shot`; сверить стоимость кадра с прежней и приложить числа
- [x] 9.4 Пройти игру руками: выбор слотов клавишами и мышью, клик по панели
      не копает, забытый над панелью курсор не мешает `Space`
- [x] 9.5 Обновить `CLAUDE.md`: строка о том, куда идти за новым слотом
      и новым глифом
