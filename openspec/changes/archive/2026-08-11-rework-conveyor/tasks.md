## 1. Модуль и числа

- [x] 1.1 Ввести `BUILD_MODULE = 4` в `config.ts` рядом с калиброванными
      величинами; вывод числа записать комментарием (вдвое ниже прежней
      секции, ≤ половины хитбокса 10, вдвое меньше просвета 8 под окном машины)
- [x] 1.2 `CONVEYOR`: `size` → `BUILD_MODULE`, `stepsPerCell` 2 → 3; убрать
      `stripePeriod` и `stripeWidth`, ввести `rollerWidth` и `rollerInset`;
      обновить обоснования в шапке (темп ≤ пятой части бега, шаг ряда меток
      равен модулю по построению)
- [x] 1.3 `SEPARATOR`: `width`/`height` 25 → 24, `window` 13 → 12; переписать
      комментарий у `window` (при чётной ширине ноги равны при чётном окне)
      и отметить, что `batch ≤ window − 2` схлопнулось в равенство
- [x] 1.4 `npm run typecheck` — убедиться, что дальше правится только то, что
      сломалось от чисел

## 2. Сетка построек

- [x] 2.1 Убрать поле `grid` из `BuildingKind` и его использования; выравнивание
      по `BUILD_MODULE` становится безусловным
- [x] 2.2 `Builder.originFor` — один путь: флор по модулю по обеим осям; ветку
      центрирования и упоминание нечётных сторон удалить
- [x] 2.3 `Builder.groundedTargetY` — возвращать уже выровненное значение через
      `ceil`; комментарием зафиксировать, почему `ceil`, а не `floor` (ошибка
      вверх годна, вниз — «занято»)
- [x] 2.4 `sectionKind` — убрать параметр размера, брать `BUILD_MODULE`
- [x] 2.5 `SEPARATOR_KIND` — снять `grid: 0`, проверить, что маска корпуса
      считается из новых размеров и ноги выходят по 6

## 3. Сторона ленты и каталог

- [x] 3.1 Добавить в `BuildingKind` поле `sideHulls: readonly [number, number] | null`
      ([влево, вправо]); `hull` остаётся стороной по умолчанию
- [x] 3.2 `entities/conveyor.ts` — один `CONVEYOR_KIND` с именем «Конвейер»
      и `sideHulls`; два прежних вида удалить, материалы `CONVEYOR_LEFT/RIGHT`
      оставить нетронутыми
- [x] 3.3 `entities/catalog.ts` — одна строка конвейера в `BUILD_CATALOG`;
      `sectionKindByHull` строит таблицу из `sideHulls ?? [hull]`
- [x] 3.4 `stampKind` — принимать материал корпуса параметром так же, как
      сейчас; убедиться, что перекладка стороны — тот же вызов с другим
      материалом
- [x] 3.5 `world/materials.ts` — подписи веществ ленты привести в соответствие
      («Конвейер влево/вправо» как материалы остаются)

## 4. Протяжка

- [x] 4.1 `core/input.ts` — модификатор стороны на `Shift` как удерживаемое
      состояние; добавить в `GAME_KEYS` только то, что нужно, не отбирая
      сочетания у системы
- [x] 4.2 `systems/builder.ts` — `BuildRun`: `begin`/`end`/`anchor`, точка
      начала в координатах мира
- [x] 4.3 `systems/builder.ts` — `Builder.line(...)`: строка из `anchor.y`,
      сторона из знака смещения (ноль — из модификатора), шаг по модулю, обрыв
      на первой негодной секции, ограничение `Digger.inReach`, перекладка
      поверх своего корпуса
- [x] 4.4 `Builder.apply` — укладывать линию по ходу жеста, а не на отпускании;
      жест нулевой длины сохраняет прежнее поведение, включая снос по попаданию
      в стоящую секцию
- [x] 4.5 `main.ts` — завести `BuildRun` рядом с каталогом, звать `begin`
      на начале применения и `end`, когда удержание кончилось; контур брать
      из той же `Builder.line`
- [x] 4.6 Проверить, что сброс ввода (потеря фокуса, оверлей) кончает жест сам,
      без отдельного признака

## 5. Вид

- [x] 5.1 `render/renderer.ts` — ролики вместо полосы: к фазе по `wx` добавить
      проверку ряда внутри секции маской `& (BUILD_MODULE − 1)`; проверка
      только внутри ветки `carry !== 0`
- [x] 5.2 `render/material-colors.ts` — цвет метки из `RAMP`, проверить
      контраст с корпусом и с пещерой
- [x] 5.3 `render/sprites/icons.ts` — перерисовать значок технологии конвейера
      по образцу оригинала: ролик с втулкой на тёмном квадрате
- [x] 5.4 Контур постройки — показывать сторону ленты (стрелка или направленная
      метка), меняющуюся вместе с модификатором
- [x] 5.5 `render/hud.ts` — над панелью показывать сторону выбранной ленты;
      у машины знака стороны не показывать

## 6. Проверки

- [x] 6.1 `tests/conveyor.ts` — размер секции равен модулю, темп ≤ пятой части
      бега, промежуток между сдвигами постоянен, каталог содержит один
      конвейер, разворот укладкой поверх, груз при развороте на месте
- [x] 6.2 `tests/conveyor.ts` — протяжка: линия целиком, строка не съезжает,
      сторона из жеста, модификатор на жесте нулевой длины, обрыв перед
      породой, обрезка дальностью, контур совпадает с поставленным
- [x] 6.3 `tests/building-placement.ts` — кратность всех видов модулю,
      выравнивание области, стыковка соседних видов, притянутое негодное место
      отвергается без сдвига
- [x] 6.4 `tests/separator.ts` — прогнать существующие проверки на 24×24;
      убедиться, что `batch ≤ window − 2` проверяется явно
- [x] 6.5 `tests/game-shell.ts` — модификатор читается как удержание, начало
      жеста отличается от продолжения, сброс кончает жест, прокладка ленты
      с клавиатуры целиком
- [x] 6.6 `tests/game-hud.ts` — сторона над панелью, её смена модификатором,
      отсутствие знака у машины
- [x] 6.7 `npm run typecheck` и `npm test` — зелёные

## 7. Снимки и осмотр

- [x] 7.1 `npm run shot` — пересчитать снимки мира, сверить числовые метрики
      с прежними (изменились размеры обеих построек)
- [x] 7.2 `npm run shot:ui` — снимки интерфейса и стоимость кадра; убедиться,
      что ролики не подорожали относительно полосы
- [x] 7.3 `npm run bench` — сравнить стоимость кадра до и после
- [ ] 7.4 Живая проверка в `npm run dev` — НЕ ВЫПОЛНЕНА. Вкладка браузера
      под автоматизацией троттлится до 1 FPS, игровой цикл в фоне не идёт,
      и заработать кредиты на открытие конвейера не удалось. Взамен покрыто
      проверками: состояние жеста (`BuildRun`) и вся линия (`Builder.line`,
      `applyLine`) в `tests/conveyor.ts`. Остаётся непроверенной вживую
      обвязка `main.ts`: `begin`/`note`/`end` по признакам ввода

## 8. Спеки

- [x] 8.1 Свести дельты в `openspec/specs/` (`/opsx:sync`) после того, как
      проверки зелёные
