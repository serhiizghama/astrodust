## 1. Подготовка

- [x] 1.1 Заархивировать изменение `rework-ui-and-action-bar`: его дельты
      на `game-hud` и `player-inventory` обязаны лечь в `openspec/specs/`
      раньше, чем на них лягут дельты этого изменения
- [x] 1.2 Убедиться, что `npm run typecheck` и `npm test` зелёные до правок —
      иначе непонятно, чей провал разбирается дальше

## 2. Одна валюта

- [x] 2.1 `src/world/materials.ts`: убрать поле `researchRate` из описания
      материала и таблицу `MAT_RESEARCH_RATE`
- [x] 2.2 `src/world/materials.ts`: дать иридию ставку кредитов 30 —
      выведена из порции сепаратора (`batch` 10 ячеек пульпы по 3 ₡ = 30 ₡,
      двукратный выигрыш = 60 ₡ на `iridium` 2 ячейки). Вывод записать
      комментарием у числа
- [x] 2.3 `src/entities/landing-module.ts`: `Payout` теряет поле очков,
      `update` начисляет только кредиты, конструктор перестаёт принимать
      `Research`, `refund` убирается
- [x] 2.4 `src/progress/research.ts`: объявить `CreditAccount`
      (`credits` + `spend`), убрать `points` и `earn`, перевести
      `status(tech, credits)` и `buy(id, account)` на кредиты
- [x] 2.5 `src/progress/technologies.ts`: цены в кредитах — 600 / 960 / 2400 /
      3000 (прежние очки × ставку иридия), пояснение к пересчёту в шапке
      таблицы
- [x] 2.6 `src/progress/index.ts`: экспортировать `CreditAccount`
- [x] 2.7 `src/render/hud.ts`: `drawCurrencies` → счётчик одной валюты;
      `src/render/sprites/icons.ts`: убрать `CRYSTAL_ICON` и ступени палитры
      значков, оставшиеся без читателя
- [x] 2.8 `src/render/renderer.ts` и `src/main.ts`: убрать `research`
      из `HudState` и из сборки снапшота
- [x] 2.9 `src/render/font.ts`: убрать глиф `✦`, если после правок он больше
      ни в одной игровой строке не встречается
- [x] 2.10 Правки проверок: `tests/harness.ts` (`IDLE_HUD`),
      `tests/landing-module.ts`, `tests/separator.ts`, `tests/game-shell.ts`,
      `tests/player-inventory.ts`, `tests/pixel-text.ts`
- [x] 2.11 `tests/landing-module.ts`: ставки монотонны вдоль цепочки
      (реголит < пульпа < иридий) и у иридия наибольшая в таблице
- [x] 2.12 `tests/separator.ts`: порция через машину даёт ровно вдвое больше
      кредитов, чем та же пульпа напрямую — сравнением с формулой, а не
      с выписанным числом
- [x] 2.13 `npm run typecheck` и `npm test` зелёные

## 3. Бесплатная постройка

- [x] 3.1 `src/entities/buildings.ts`: убрать `cost` из `BuildingKind`
      и из `sectionKind()`
- [x] 3.2 `src/entities/separator.ts`, `src/entities/conveyor.ts`: убрать цену
      из описаний видов; `src/config.ts`: убрать `SEPARATOR.cost`
      и `CONVEYOR.sectionCost`
- [x] 3.3 `src/systems/builder.ts`: убрать `'funds'` из `PlacementIssue`,
      параметр `credits` из `preview` и `issueAt`, списание в `apply`
      и возвраты в `demolish` и сносе секции
- [x] 3.4 `src/main.ts`: `placementIssueText` без ветки нехватки средств,
      строка над панелью — вид постройки без цены
- [x] 3.5 Правки проверок: `tests/building-placement.ts`, `tests/conveyor.ts`,
      `tests/shot.ts`, `tests/bench-render.ts`
- [x] 3.6 `tests/building-placement.ts`: постановка и снос не меняют счёт;
      при нулевом счёте место годно и постройка появляется; открытый вид
      ставится подряд без ограничения по числу
- [x] 3.7 `npm run typecheck` и `npm test` зелёные

## 4. Раскладка дерева

- [x] 4.1 `src/progress/tech-layout.ts`: колонка узла — длина самой длинной
      цепочки предпосылок, строка — порядок в таблице внутри колонки; список
      рёбер парами индексов; габариты сетки
- [x] 4.2 `src/progress/tech-layout.ts`: `stepTo(index, dx, dy)` — по вертикали
      соседний узел колонки с упором в край, по горизонтали ближайший по строке
      узел соседней колонки (при равенстве — меньшая строка)
- [x] 4.3 `src/progress/technologies.ts`: поле `icon` у записи технологии —
      ключ значка, а не спрайт: таблица про пиксели ничего не знает
- [x] 4.4 `src/progress/research.ts`: `ResearchOverlay` переходит на двумерную
      навигацию через `stepTo`; `handle` принимает узел под курсором и признак
      нажатия мыши и покупает узел под курсором, а не выбранный
- [x] 4.5 `src/core/input.ts`: `menuLeftPressed` / `menuRightPressed`
      на `A`/`←` и `D`/`→`
- [x] 4.6 `src/progress/index.ts`: экспортировать раскладку и её типы
- [x] 4.7 `tests/research.ts`: колонка равна глубине; узлы не накладываются;
      каждое ребро идёт слева направо; раскладка повторяема; габариты
      помещаются в панель опорного кадра
- [x] 4.8 `tests/research.ts`: покупка за кредиты — списание, нехватка,
      однократность, предпосылка; отвергнутая покупка не стоит ни кредита
- [x] 4.9 `tests/research.ts`: навигация упирается в края и не заворачивается;
      весь цикл «открыть — дойти до любого узла — купить — закрыть» проходится
      одними клавишами
- [x] 4.10 `npm run typecheck` и `npm test` зелёные

## 5. Отрисовка дерева

- [x] 5.1 `src/render/draw.ts`: `hLine` и `vLine` — обёртки над `fillRect`
- [x] 5.2 `src/render/sprites/icons.ts`: значки технологий 12×12 по ключам
      таблицы плюс заметный запасной для неизвестного ключа
- [x] 5.3 `src/config.ts`: блок `TECH_TREE` — сторона узла 18, значок 12,
      шаг колонки 44, шаг строки 32, с выводом каждого числа
- [x] 5.4 `src/render/overlay.ts`: `OverlayView` получает `col`, `row`, `icon`,
      `kind`, `cost`, `description` у строк, список рёбер и наведённый узел
- [x] 5.5 `src/render/overlay.ts`: `techTreeLayout(viewW, viewH)` — панель
      и начало центрированной сетки; `nodeAtPoint` — номер узла под точкой
- [x] 5.6 `src/render/overlay.ts`: отрисовка связей коленом из `hLine`/`vLine`,
      цвет по тому, открыта ли предпосылка
- [x] 5.7 `src/render/overlay.ts`: узлы — подложка по состоянию покупки, рамка
      по виду эффекта, значок внутри, цена подписью под узлом; кольца выбора
      и наведения разными цветами
- [x] 5.8 `src/render/overlay.ts`: подсказка — имя, пояснение, цена, состояние
      словами; источник — наведённый узел, иначе выбранный; отражается от края,
      рисуется последней
- [x] 5.9 `src/render/index.ts`: опубликовать `techTreeLayout`, `nodeAtPoint`
      и их типы
- [x] 5.10 `src/main.ts`: в состоянии оверлея считать узел под курсором,
      передавать его в оверлей и в снапшот; собрать строки дерева с раскладкой
- [x] 5.11 `tests/research.ts`: узел постройки и узел навыка отличаются рамкой
      в любом состоянии покупки; четыре состояния покупки отличаются подложкой;
      попарная различимость проверяется как в проверках палитры
- [x] 5.12 `tests/research.ts`: цена видна у некупленного узла без наведения;
      подсказка называет недостающую сумму и неоткрытые предпосылки поимённо;
      подсказка целиком внутри панели для узлов у любого края
- [x] 5.13 `tests/research.ts`: `nodeAtPoint` попадает в тот же узел, который
      нарисован; нажатие мимо узлов ничего не покупает
- [x] 5.14 `npm run typecheck` и `npm test` зелёные

## 6. Завершение

- [x] 6.1 `npm run shot`: сцена с открытым деревом — узлы, связи, подсказка;
      сверить числовые метрики
- [x] 6.2 `npm run bench`: стоимость кадра с открытым оверлеем не выросла
      заметно против прежнего списка
- [x] 6.3 `openspec/specs/research/spec.md` и
      `openspec/specs/building-placement/spec.md`: поправить `## Purpose` —
      он говорит про очки и про постройку за кредиты (дельта Purpose
      не переносит)
- [x] 6.4 `CLAUDE.md`: поправить карту и таблицу «куда идти за изменением» —
      новый лист раскладки в `progress/`, значок технологии в
      `render/sprites/icons.ts`, у постройки больше нет цены
- [x] 6.5 `npm run build` и `npm test` зелёные, `npm run format`

## 7. По итогам просмотра в игре

- [x] 7.1 `src/render/overlay.ts` + `src/render/sprites/icons.ts`: курсор меню
      стрелкой поверх всего содержимого. Мировой прицел рисуется ДО оверлея
      и уходит под непрозрачную подложку — в меню его не было вовсе
- [x] 7.2 `src/render/overlay.ts`: название и цена подписью под узлом; шаг
      колонки выведен из самой длинной подписи таблицы
- [x] 7.3 `src/progress/technologies.ts`: поле `usage` — какой режим, какая
      клавиша, что именно меняется. Отдельно от `description`: «что это»
      и «что с этим делать» — разные вопросы
- [x] 7.4 `src/render/overlay.ts`: плавающая подсказка заменена полосой
      сведений внизу панели. Коробка у узла закрывала соседей и связи,
      а без мыши висела над деревом постоянно
- [x] 7.5 `tests/research.ts`: курсор нарисован и следует за мышью; курсор
      поверх полосы; полоса не наезжает на дерево; название помещается в шаг
      колонки; строки полосы помещаются в её ширину
- [x] 7.6 Дельта `specs/research`: узел показывает название, полоса сведений
      вместо подсказки, поле применения в таблице, курсор в меню
- [x] 7.7 `npm run typecheck`, `npm test`, `npm run shot`, `npm run build`
