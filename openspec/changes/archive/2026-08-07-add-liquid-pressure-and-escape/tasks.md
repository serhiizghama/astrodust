## 1. Карта уровня

- [x] 1.1 Завести в `World` счётчик жидких ячеек `liquidCells` по образцу `gasCells` — обновляется в том же месте, что и он
- [x] 1.2 Завести карту уровня `Uint16Array` размером с сетку, выделяемую лениво при первом появлении жидкости; `0xffff` — «уровня нет»
- [x] 1.3 Развёртка A (сверху вниз) по активным чанкам: над ячейкой пустота → уровень `y`; над ячейкой тот же материал → уровень сверху; над ячейкой твёрдое → «уровня нет»
- [x] 1.4 Два прохода по строке влево и вправо: минимум уровня по непрерывному участку одного материала
- [x] 1.5 Развёртка B (снизу вверх): перенос уровня снизу и те же два прохода по строке — без неё напор не обходит перемычку
- [x] 1.6 Пропускать расчёт целиком, когда `liquidCells === 0`

## 2. Подъём под напором

- [x] 2.1 Отдельный проход сверху вниз: ячейка с `y − уровень ≥ 2`, которой некуда идти вниз, по диагоналям и вбок, поднимается на одну строку
- [x] 2.2 Порядок целей подъёма: прямо вверх, затем верхние диагонали по той же детерминированной стороне, что и в остальных правилах
- [x] 2.3 Поставить проход в шаге после падения и перед газами; убедиться, что за шаг ячейка поднимается не более чем на одну строку
- [x] 2.4 Зафиксировать замером, почему порог 2, чтобы его не «упростили» обратно: с порогом 1 сосуды выравниваются, но водоём не засыпает — 20480 обойдённых ячеек за пять шагов покоя против нуля

## 3. Покой жидкости

- [x] 3.1 Ячейка свободной поверхности (`y == уровень`) смещается вбок, только если с новой позиции сможет сойти вниз
- [x] 3.2 Ячейка под напором и ячейка без уровня (под крышкой) растекаются без этого условия — иначе вода не пройдёт щель высотой в одну ячейку
- [x] 3.3 Довести замер «обойдено ячеек после 4000 шагов отстоя» до нуля на трёх сценах: чаша со стенками, открытый пол, сообщающиеся сосуды
- [x] 3.4 Гейт пройден: ноль достигнут на всех четырёх сценах, к спеке возвращаться не пришлось
- [x] 3.5 Убедиться, что правило не отменило растекание: лужа в углублении по-прежнему доходит до ровного уровня

## 4. Текучесть вместо нулевой растекаемости

- [x] 4.1 Добавить в материал поле `flow` (текучесть) 0..1 и развёрнутый массив `MAT_FLOW`
- [x] 4.2 Гейт в правиле движения жидкости через тот же `chance(x, y)`, что и осыпаемость у сыпучего
- [x] 4.3 Вода: `flow = 1`. Лава: подобрать `flow` и ненулевую растекаемость замером ширины разлива за фиксированное число шагов
- [x] 4.4 Проверить, что силуэт кучи лавы перестал совпадать с силуэтом кучи рыхлого реголита — сейчас они совпадают ячейка в ячейку
- [x] 4.5 Проверить, что лава на большом числе шагов всё-таки выравнивается, а не остаётся конусом

## 5. Персонаж и вещество

- [x] 5.1 Снять запрет на вход в область персонажа для жидкостей и газов, оставить его для сыпучего
- [x] 5.2 Лаве выставить `blocksPlayer: false`
- [x] 5.3 Продавливание при блокировке шага вбок: обмен содержимого покидаемой и занимаемой колонок ячейка в ячейку
- [x] 5.4 То же для шага вверх — обмен строк; продавливание NOT применяется к падению под гравитацией
- [x] 5.5 Отказ от продавливания, если хоть одна ячейка входящей колонки или строки статична
- [x] 5.6 Константа интервала продавливания в `config.ts` с пояснением, откуда взято значение
- [x] 5.7 Отладочная кисть получает область персонажа и пропускает её ячейки; передать область из `main.ts`

## 6. Проверки

- [x] 6.1 Сообщающиеся сосуды: перепад уровней сходится к нулю-единице, вода не создаётся и не исчезает
- [x] 6.2 Жидкость не поднимается выше своего уровня: лужа у высокой стены не карабкается по ней и не переливается
- [x] 6.3 Подъём не быстрее одной ячейки за шаг
- [x] 6.4 Улёгшийся водоём засыпает: обойдено ячеек — ноль, ни одна ячейка не меняет положения
- [x] 6.5 Поток жидкости проходит сквозь стоящего на пути персонажа и растекается по обе стороны
- [x] 6.6 Персонаж, обложенный рыхлым реголитом со всех сторон, выбирается за конечное время; количество реголита при этом не изменилось
- [x] 6.7 Персонаж не продавливается сквозь породу и не тонет в куче, на которой стоит
- [x] 6.8 Продавливание заметно медленнее ходьбы по твёрдому — сравнение времени на одинаковой дистанции
- [x] 6.9 Отладочная установка в себя оставляет ячейки хитбокса пустыми
- [x] 6.10 Переписать проверку «вязкая жидкость остаётся кучей» на «выравнивается медленнее» и «в итоге всё-таки выравнивается»
- [x] 6.11 Повторяемость: прогон с жидкостями, напором и подъёмом дважды из одного состояния даёт идентичные сетки
- [x] 6.12 Прогнать весь существующий набор — автомат, чанки, движение, полёт, копание, камера не должны сломаться

## 7. Завершение

- [x] 7.1 Замер стоимости до и после на сцене с водой: обойдено ячеек за шаг в движении и в покое
- [x] 7.2 Проверить сборку: `tsc` без ошибок, продакшн-билд собирается, `npm test` зелёный
- [x] 7.3 Живая проверка в браузере: вода ложится ровной плёнкой по рельефу, лава — плоской лужей, персонаж проходит сквозь неё и стоит ВНУТРИ, а не сверху. Плюс безголовый рендер тех же двух сцен со скриншотов: `shots/liquid-water-tunnel.png`, `shots/liquid-lava-flow.png`
- [x] 7.4 Живая проверка засыпания: полный завал воспроизведён и пройден вручную владельцем проекта. Со стороны агента дополнительно проверено, что после высыпания реголита себе на голову персонаж сразу уходит вправо и вверх, а выход из полного завала покрыт автотестом (1.02 с)
- [x] 7.5 Обновить `docs/02-mvp-plan.md`: текучесть и интервал продавливания в списке констант
