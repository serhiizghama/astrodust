## 1. Буфер: комок из одного вещества

- [x] 1.1 В `src/entities/grab.ts` заменить `Uint32Array` счётчиков на пару
      «вещество + количество»: поля `material: number | null` и `count`,
      геттеры `used`, `free`, `isEmpty`, `isFull` сохранить.
- [x] 1.2 `add(material, amount)` возвращает 0, если буфер не пуст и вещество
      не совпадает; при пустом буфере задаёт вещество. Правило однородности
      живёт здесь и больше нигде.
- [x] 1.3 `take(amount)` без параметра вещества; опустевший буфер сбрасывает
      `material` в `null`.
- [x] 1.4 Убрать `materials()`; переписать шапку модуля под новое устройство
      (смеси нет — обосновывать её порядок больше нечего).
- [x] 1.5 Обновить публичную поверхность `src/entities/index.ts`, если состав
      экспортов изменился.

## 2. Инструмент: жест вместо решения по нажатию

- [x] 2.1 В `src/systems/grabber.ts` убрать латч штриха (`stroke`) и параметр
      `pressed`; ввести состояние жеста: флаг «жест идёт» и тип комка.
- [x] 2.2 Начало жеста (`held` при отсутствии флага) латчит тип: непустой
      буфер — его вещество; пустой — вещество ячейки под перекрестием, если
      оно переносимо, иначе типа нет.
- [x] 2.3 Удержание набирает только ячейки латченого типа своим интервалом;
      полный буфер жест не прерывает.
- [x] 2.4 Конец жеста (флаг есть, `held` ложно) выбрасывает содержимое буфера
      по плану выброса; не поместившееся остаётся в буфере.
- [x] 2.5 Добавить параметр `blocked`: при нём шаг не набирает, не выбрасывает
      и не трогает флаг жеста.
- [x] 2.6 Удалить проверку `Digger.inReach` и импорт `Digger`, если он больше
      не нужен; состояния «недостижимо» у захвата нет.
- [x] 2.7 `planTake` фильтрует по типу комка вместо `MAT_PORTABLE`-ветки
      «любое переносимое»; `canTake` привести к новому правилу или убрать.
- [x] 2.8 `applyDrop` упростить до одного прохода по ячейкам плана: разбора
      смеси больше нет.
- [x] 2.9 Переписать блок ограничений в шапке модуля под жест и отсутствие
      дальности.

## 3. Цель захвата и жест в шаге игры

- [x] 3.1 В `src/main.ts` завести отдельную цель захвата от активного
      источника прицела: `actionTarget(input.aimSource === 'mouse', …)`;
      общая `aim` остаётся у копания, сбора и отладочной кисти.
- [x] 3.2 Передать в `grabber.update` `held` и `blocked`
      (`aimSource === 'mouse' && input.overUi`), убрать `pressed`.
- [x] 3.3 `grabPlanView` строить по новой цели захвата; добавить в `GrabView`
      вещество комка и количество несомого.
- [x] 3.4 Строку «что несу» и `HudState.grabHeld` собрать из одного вещества
      буфера (список из нуля или одного элемента — форма `HudState`
      не меняется).

## 4. Отрисовка комка

- [x] 4.1 Дать `render/material-colors.ts` базовый цвет вещества по id
      (через уже существующие таблицы ступеней) и экспортировать его
      в `render/index.ts`, если он нужен снаружи.
- [x] 4.2 В `drawGrab` при решении «выброс» и непустом буфере заливать ячейки
      плана сплошным цветом вещества вместо синего дизеринга; подсветку
      набора оставить дизерингом.
- [x] 4.3 Убрать из отрисовки захвата признак недостижимой цели, если он там
      был; квадрат рисуется всегда там, куда целится игрок.

## 5. Проверки

- [x] 5.1 В `tests/matter-grabber.ts` заменить помощник `press` на помощники
      жеста: «зажать», «вести удерживая», «отпустить».
- [x] 5.2 Удалить сценарии «Недостижимая цель», «Промах не тратит ход»,
      «Выброс не повторяется удержанием», «Комок бывает смешанным».
- [x] 5.3 Добавить проверки жеста: зажал — набирает; ведение добирает;
      отпустил — выбросил; полный буфер жест не прерывает; отпускание
      при `blocked` комок не роняет; пустой буфер отпусканием ничего
      не делает; остаток донашивается.
- [x] 5.4 Добавить проверки однородности: тип задаёт ячейка под перекрестием;
      второе вещество не добирается; перекрестие мимо вещества не берёт
      ничего.
- [x] 5.5 Добавить проверку «дальности нет»: набор и выброс за пределом
      `DIG.reach` меняют мир так же, как вплотную.
- [x] 5.6 Сохранить и при необходимости переписать под новый API проверки
      сохранения вещества, границы ёмкости, выброса только в пустоту,
      выброса в себя, повторяемости сетки и сдачи через приёмник.
- [x] 5.7 Обновить вызовы `Grabber.update` и `Grab` в `tests/player-inventory.ts`
      под новую сигнатуру; проверка «высыпание не трогает захват» должна
      остаться зелёной.
- [x] 5.8 Добавить проверку, что квадрат захвата следует за курсором:
      при `aimSource === 'mouse'` и отпущенной кнопке цель плана совпадает
      с ячейкой под курсором, а не с ячейкой у персонажа.

## 6. Прогон

- [x] 6.1 `npm run typecheck` — чисто.
- [x] 6.2 `npm test` — все наборы зелёные.
- [x] 6.3 `npm run shot` — снимки мира собираются без ошибок.
- [x] 6.4 Проверить в `npm run dev`: комок виден у курсора, переносится через
      весь кадр, отпускание над панелью его не роняет.
