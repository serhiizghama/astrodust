## 1. Что сделал захват — наружу

- [x] 1.1 В `src/systems/grabber.ts` завести состояние прошедшего шага:
      `lastAction: 'take' | 'drop' | 'none'` и число изменённых ячеек.
      Обнуляется в начале каждого `update`, включая ветку `blocked`.
- [x] 1.2 Проверить, что `update` по-прежнему возвращает число ячеек —
      существующие вызовы не трогаются.

## 2. Сигналы и дорожка захвата

- [x] 2.1 В `src/config.ts` добавить блок `AUDIO.grab`: громкости шороха
      и сброса, полосы (ниже `dust.hzQuiet`), добротность, спады, потолок
      темпа шорохов, размер порции для выхода на предел.
- [x] 2.2 В `src/audio/signals.ts` добавить `grabTaken`, `grabDropped`
      и одну пару `grabX`/`grabY`; обнулять счётчики в `resetSignals`.
      Инвариант «набор и выброс не бывают на одном шаге» назвать в комментарии
      и подпереть тестом.
- [x] 2.3 Написать `src/audio/voices/grab.ts` по образцу `voices/dig.ts`:
      модель (чистые функции над сигналами) плюс голос. Шорох — порция набора,
      сброс — отпускание.
- [x] 2.4 Громкость шороха вывести из числа ячеек порции; порции сверх потолка
      темпа вливать в следующий шорох, а не порождать голос.
- [x] 2.5 Сброс — один голос на событие, тише шороха, ниже по полосе.
- [x] 2.6 Добавить дорожку строкой в `createVoices()` и экспорт модели
      в `src/audio/index.ts` рядом с `dig`/`dust`.
- [x] 2.7 В `src/main.ts` заполнить сигналы захвата из `grabber.lastAction`
      и цели захвата — в том же месте, где собираются остальные счётчики,
      до `input.endStep()`.

## 3. Подпись вещества у прицела

- [x] 3.1 В `src/main.ts` считать вещество ячейки под перекрестием и класть
      в снапшот кадра вместе с признаком переносимости; вне режима захвата —
      `null`.
- [x] 3.2 В `src/config.ts` добавить кегль и отступ подписи в блок `UI`.
- [x] 3.3 Нарисовать подпись в слое интерфейса у экранной позиции курсора:
      светлый тон для переносимого, приглушённый для остального, ничего
      для пустоты.
- [x] 3.4 У правого края кадра переносить подпись влево от крестика —
      по измеренной ширине, а не по константе.

## 4. Проверки

- [x] 4.1 `tests/procedural-audio.ts`: шорох звучит на порции набора и молчит
      на удержании без набора.
- [x] 4.2 Громкость шороха растёт с размером порции; потолок темпа не
      превышается при наборе каждый шаг.
- [x] 4.3 Сброс звучит один раз на отпускание и тише шороха; отпускание
      без единой размещённой ячейки не звучит.
- [x] 4.4 Набор дальше радиуса контактной слышимости не слышен.
- [x] 4.5 Полоса захвата не пересекается с полосой пыли.
- [x] 4.6 Набор и выброс не публикуются на одном шаге.
- [x] 4.7 `tests/matter-grabber.ts`: вещество под перекрестием сообщается
      кадру и совпадает с тем, что уйдёт в буфер; над пустотой — `null`;
      вне режима захвата — `null`.
- [x] 4.8 `tests/game-hud.ts` или `tests/ui-layer.ts`: подпись не выходит
      за правый край кадра.

## 5. Прогон

- [x] 5.1 `npm run typecheck` — чисто.
- [x] 5.2 `npm test` — все наборы зелёные.
- [x] 5.3 `npm run verify:audio` — офлайн-рендер звука проходит.
- [x] 5.4 `npm run shot:ui` — кадр интерфейса собирается.
