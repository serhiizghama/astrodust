## 1. Числа и буфер

- [ ] 1.1 `src/config.ts`: блок `GRAB` со стороной квадрата 13 и интервалом,
      равным `DIG.interval`. В шапке блока — оба инварианта стороны
      (`side ≤ 2·DIG.radius + 1`, `DIG.aimDistance ≤ hitboxW/2 + (side−1)/2`)
      и указание на их страж `tests/matter-grabber.ts`. Ёмкость отдельным
      числом не писать: она выводится из стороны.
- [ ] 1.2 `src/entities/grab.ts`: класс `Grab` — счётчики по id материала,
      предел `side²`, `add`/`take`/`count`/`used`/`free`, перечисление
      непустых веществ для кадра. Выбранного вещества у него нет: выбрасывается
      всё.
- [ ] 1.3 `src/entities/index.ts`: экспорт `Grab`.

## 2. Инструмент

- [ ] 2.1 `src/systems/grabber.ts`: `Grabber.plan(...)` — один обход квадрата,
      заполнение переиспользуемого `Int16Array` парами координат, возврат
      `action`/`count`. Дальность проверяется до всего остального. Переносимость
      читается из `MAT_PORTABLE` — своего списка веществ здесь нет.
- [ ] 2.2 `Grabber.update(...)`: латч штриха на `toolPressed`, сброс на
      отпускании, набор по интервалу при удержании, выброс один раз на нажатие.
      План для подсветки считается с `forced = stroke`.
- [ ] 2.3 Выброс: только пустые ячейки, вне хитбокса персонажа, порядок веществ
      по возрастанию id, ячейки построчно. Не поместившееся остаётся в буфере.
- [ ] 2.4 Набор: запись через `world.set` (пробуждение чанка), остановка
      по заполнению буфера, частичный забор на границе ёмкости.
- [ ] 2.5 `src/systems/index.ts`: экспорт `Grabber` и типа плана.

## 3. Режим, слот и технология

- [ ] 3.1 `src/core/input.ts`: `ToolMode.Grab`; `ACTION_SLOTS` становится
      таблицей `{ mode, requires }` в порядке копание, захват, стройка, сбор;
      у сбора `requires` — идентификатор содержимого пылесоса.
- [ ] 3.2 `ActionBarState`: принимает `ContentUnlocks` (умолчание `NO_UNLOCKS`),
      заводит `available(slot)`, `select` и `cycle` его спрашивают. Геттер
      `grabbing` рядом с `digging`/`collecting`/`building`.
- [ ] 3.3 `src/progress/technologies.ts`: `CONTENT.VACUUM`; строка
      «Вакуумный пылесос», 600 ₡, без предпосылок, эффект `unlock`;
      `wide-nozzle.requires = ['vacuum']`. Комментарий цены — в тех же порциях
      сепаратора, что и у остальных строк.
- [ ] 3.4 Проверить, что раскладка графа пересчиталась из предпосылок сама
      (`tech-layout` руками не трогать) и дерево осталось ацикличным.

## 4. Кадр

- [ ] 4.1 `src/render/renderer.ts`: `GrabView` в снапшоте кадра; подсветка ячеек
      попиксельно в буфер мира через `BAYER` — `RAMP.green[4]` на наборе,
      `RAMP.blue[4]` на выбросе; контур квадрата вместо кольца в режиме захвата.
- [ ] 4.2 Заполнение буфера по периметру квадрата: ярким тоном первые
      `round(used/capacity · периметр)` пикселей, остальные тусклым.
- [ ] 4.3 `src/render/sprites/icons.ts`: значок захвата; убедиться, что значок
      сбора остаётся узнаваемым в приглушённом виде.
- [ ] 4.4 `src/render/hud.ts`: третье состояние слота (доступен / закрыт /
      пуст) — приглушённая заливка и значок на месте; `SlotAction` получает
      `'grab'`, `HudSlot` — признак закрытости.
- [ ] 4.5 Строка над панелью: состав захвата с пределом всегда; заполненность
      инвентаря и выбранное вещество — только когда пылесос открыт.

## 5. Сборка

- [ ] 5.1 `src/main.ts`: создать `Grab` и `Grabber`, передать `research`
      в `ActionBarState`, применить захват в фазе инструментов, положить
      `GrabView` и состав буфера в снапшот кадра.
- [ ] 5.2 `HUD_SLOTS` собирается из новой таблицы слотов; признак закрытости
      считается на кадр, а не один раз при старте, — покупка обязана его снять.

## 6. Проверки

- [ ] 6.1 `tests/matter-grabber.ts`: форма и симметрия квадрата; оба инварианта
      стороны; переносимость (реголит берётся, порода, вода, лава и пар — нет);
      сохранение вещества; частичный забор на границе ёмкости; смешанный комок.
- [ ] 6.2 `tests/matter-grabber.ts`: решение по цели — по веществу берёт,
      по пустоте кладёт, при полном буфере кладёт даже над веществом; латч
      держится до отпускания; выброс не повторяется удержанием; промах не тратит
      ход; недостижимая цель не меняет ни мир, ни буфер.
- [ ] 6.3 `tests/matter-grabber.ts`: выброс только в пустоту, мимо хитбокса,
      остаток остаётся в буфере, повторяемость сетки; сдача через зону приёмника
      начисляет кредиты без отдельного правила.
- [ ] 6.4 `tests/matter-grabber.ts`: план подсветки и применённые ячейки
      совпадают — тот же вызов, тот же результат.
- [ ] 6.5 `tests/player-inventory.ts`: сбор закрыт до покупки; списки
      переносимого у сбора и захвата совпадают; высыпание не трогает буфер
      захвата; перебор и прямой выбор обходят закрытый слот; покупка открывает
      режим сразу.
- [ ] 6.6 `tests/research.ts`: раструб требует пылесоса; дерево ациклично;
      профиль до покупок равен базовому; первая покупка достижима за небольшое
      число полных комков (счёт по ставке реголита).
- [ ] 6.7 `tests/game-hud.ts`: порядок слотов; три состояния слота различимы;
      закрытый слот не выбирается и не попадает в перебор; клавиша слота
      не переезжает после покупки; строка над панелью до и после открытия
      пылесоса.
- [ ] 6.8 `npm run typecheck` и `npm test` — зелёные. `npm run bench`:
      стоимость кадра не выросла заметно.
- [ ] 6.9 `npm run shot` и `npm run shot:ui` — перегенерировать снимки:
      порядок слотов и вид закрытого слота изменились.

## 7. Свод

- [ ] 7.1 `CLAUDE.md`: строка таблицы про новый инструмент и про то, что режим
      может быть закрыт технологией; упоминание `entities/grab.ts`
      и `systems/grabber.ts` в карте.
- [ ] 7.2 При архивации: требование `game-hud` «Низ кадра занят панелью
      и состоянием инвентаря» правится и в `rework-conveyor`. Если то изменение
      уже применено — пересобрать копию требования поверх него, а не поверх
      исходной.
