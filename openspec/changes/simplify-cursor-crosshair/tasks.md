## 1. Прицел

- [ ] 1.1 В `src/render/renderer.ts` переписать `drawAim`: подпись
      `drawAim(sx, sy, inReach)`, кольцо не рисуется, лучи `[-3, -2, 2, 3]`
      по обеим осям цветом `green[4]` / `gray[5]`, ядро в центре при `inReach`
- [ ] 1.2 Переписать шапку `drawAim`: убрать объяснение кольца и различия
      режимов; оставить утверждениями то, что осталось верным — почему цвет
      значит достижимость и почему у лучей есть разрыв вокруг центра
- [ ] 1.3 Поправить вызов `this.drawAim(...)` (стр. ~397): передавать три
      аргумента, `hud.collecting` и `hud.collectRadius` больше не идут в прицел

## 2. Удаление геометрии предпросмотра

- [ ] 2.1 Удалить из `src/render/renderer.ts`: `brushOutline`, `BRUSH_OUTLINE`,
      `VACUUM_OUTLINE`, `vacuumOutline`, `OUTLINE_CACHE` и их комментарии
- [ ] 2.2 Убрать `BRUSH_OUTLINE`, `VACUUM_OUTLINE`, `vacuumOutline`
      из экспортов `src/render/index.ts`
- [ ] 2.3 Проверить, что импорты `DIG`/`VACUUM` в `renderer.ts` ещё нужны;
      если радиусы читались только контуром — убрать и их

## 3. Проверки

- [ ] 3.1 `tests/terrain-digging.ts`: удалить блок проверок контура кисти
      («не выходит за область выемки», «периметр, а не заливка» и соседние)
      вместе с импортом `BRUSH_OUTLINE`
- [ ] 3.2 `tests/player-inventory.ts`: свести проверку «кисть сбора не больше
      копательной» к сравнению радиусов, убрать условие по длинам контуров
      и импорты `BRUSH_OUTLINE` / `VACUUM_OUTLINE`
- [ ] 3.3 `tests/research.ts`: удалить проверку «кольцо прицела растёт вместе
      с кистью сбора» и импорт `vacuumOutline`; проверку роста самого радиуса
      оставить нетронутой
- [ ] 3.4 Добавить в `tests/terrain-digging.ts` проверку формы прицела:
      достижимая цель даёт закрашенный центр, недостижимая — пустой,
      и вид не зависит от режима инструмента

## 4. Спеки и соседние документы

- [ ] 4.1 Поправить `openspec/changes/add-matter-grabber/design.md`: `drawAim`
      больше не «рисует кольцо кисти и крестик»
- [ ] 4.2 Свериться, что в `openspec/specs/` не осталось других упоминаний
      контура кисти помимо тех, что покрыты дельтами этого изменения

## 5. Проверка результата

- [ ] 5.1 `npm run typecheck` — чисто
- [ ] 5.2 `npm test` — чисто
- [ ] 5.3 `npm run shot` — снять снимок и убедиться глазами, что в кадре
      остался один крестик без обводки; при падении пороговой проверки снимка
      пересчитать порог
